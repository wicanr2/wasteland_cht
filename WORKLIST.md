# Remake 工項清單

這一份是**「要做什麼」的單一清單**，含已完成的。與其他三份的分工：

| 檔案 | 回答什麼 |
|---|---|
| [`CONTEXT.md`](CONTEXT.md) | 現況、術語、已被推翻的斷言（接手時先讀它） |
| [`docs/re/00-master-index.md`](docs/re/00-master-index.md) | 原版怎麼做（事實速查） |
| [`docs/re/00-wiring-status.md`](docs/re/00-wiring-status.md) | 每份 RE 筆記接上了沒（`TestWiringStatus` 守著） |
| **本檔** | **remake 還缺哪些功能** |

狀態只有三種：**完成**（實作 ＋ 驗收證據）、**部分**（能跑但缺一段，缺什麼寫清楚）、
**未開始**。⚠ **不寫「暫緩」「低投報」**——卡住就寫卡在哪、下一個入口是什麼
（`CLAUDE.md` §5）。

⚠ **驗收不走「從頭玩到結局」那條路**（使用者定案 2026-08-15）：
一次通關要幾十小時、不可重跑，中途任何一步卡住就什麼都驗不到。
改成**機制與劇本觸發全部接上 ＋ 抽樣驗證**——與腳本、遭遇、設施、戰鬥
那幾把尺同一套做法（見 §7）。

最後更新：2026-08-17（命中基礎值 `101`、走不到的 opcode `102`、名片行兩欄 `103`、opcode 2 `104`、`ENC` 空地回合 `105`）。

---

## 1. 資產與資料層

| # | 工項 | 狀態 | 依據／證據 |
|---|---|---|---|
| 1.1 | 執行檔解包（EXEPACK）＋ overlay 合成 | **完成** | `docs/re/02`、`04`；`wl.merged.exe` |
| 1.2 | 資源目錄與 42 個 MSQ 區塊 | **完成** | `docs/re/06`、`07`；`internal/assets/msq.go` |
| 1.3 | MSQ 解密（XOR ＋ checksum） | **完成** | `docs/re/08`（42/42） |
| 1.4 | Huffman 解壓 | **完成** | `docs/re/11`（173 塊） |
| 1.5 | 三層地圖 | **完成** | `docs/re/24`；42/42 通過值域檢查 |
| 1.6 | 記錄區與 section 定址 | **完成** | `docs/re/16`、`18` |
| 1.7 | 圖片與圖磚（packed 4bpp ＋ 列間 XOR） | **完成** | `docs/re/23`；`TITLE.PIC` 與實機逐像素 100% 吻合 |
| 1.8 | EGA 調色盤 | **完成** | `docs/re/23` §7：原版從不設調色盤，用 mode 0Dh 預設 16 色 |
| 1.9 | 字型（`COLORF.FNT`、`IC0_9.WLF`、`MASKS.WLF`） | **完成** | `docs/re/14`、`24` |
| 1.10 | 壓縮文字與字串表 | **完成** | `docs/re/17`、`18` |
| 1.11 | 存檔格式（讀 ＋ byte-for-byte 寫回） | **完成** | `docs/re/30`、`45`；`docs/re/82` 的 round-trip 門檻 |
| 1.12 | 圖片局部動畫 | **完成** | 規格 26；`docs/re/23` §5 |
| 1.13 | `END.CPA` 解碼 | **完成** | **兩段都解了也都接上了**。第一段是畫面：整份就是 Huffman（`Decompress` 自己讀長度欄），0x4800 bytes ＝ 288×128 packed 4bpp ＋ 列間 XOR delta（`docs/re/23` §9）。第二段 `0x3A98` bytes **是 15 格的動畫腳本**不是敘述（`docs/re/96` §3），結局播放已經在用 |
| 1.14 | `CURS` 與滑鼠 | **完成** | 規格 29。游標解出來畫在指標上（`internal/assets/curs.go`），控制**走 remake 自己的路**（使用者定案）：點地圖走一步、點指令列下指令、點清單選那一項、右鍵取消。**哪個圖形對應哪個狀態解完了**（`docs/re/112`、T34）：`ds:8DCDh` 是索引，0 預設／1 可點／2–5 上下左右／6 中央方框，第 7 個原版選不到。⚠ 還沒解：32 寬與 slot 21 的 24 寬為何對不上 |
| 1.15 | `TRANSTBL` 的用途 | **完成（解到底）** | 形狀已解（50 組 × 16，載到 `seg003:0x0100`）；**「沒有人讀它」是有證據的結論不是放棄**——三層排除掃過（`docs/re/56` §3）。接不上去是因為原版就沒有消費端 |

## 2. 規則層

| # | 工項 | 狀態 | 依據／證據 |
|---|---|---|---|
| 2.1 | 亂數與擲骰 | **完成** | 規格 02；`docs/re/13` |
| 2.2 | 移動、四道閘、遊戲時鐘 | **完成** | 規格 04；`docs/re/26`、`27`、`62` |
| 2.3 | 角色記錄與七個屬性 | **完成** | 規格 05；`docs/re/15`、`21` |
| 2.4 | 命中判定 | **完成** | `docs/re/88`：基礎 ＋ Brawling×3 ＋ Agility − 對手行動值 |
| 2.5 | 傷害、護甲、傷勢等級 | **完成** | 規格 06；`docs/re/19`、`20` |
| 2.6 | 回合結構與行動順序 | **完成** | `docs/re/90`：兩邊公式不同；只有下攻擊令的人進行動表 |
| 2.7 | 戰鬥指令與逃跑 | **完成** | 規格 14；`docs/re/38` |
| 2.8 | 敵人目標選擇 | **完成** | `docs/re/89`：隨機重抽，倒下的跳過 |
| 2.9 | 遭遇掃描與生成 | **完成** | 規格 13、15；`docs/re/39`、`78` |
| 2.10 | 敵人血量與經驗值 | **完成** | `docs/re/37`、`32` |
| 2.11 | 技能檢定與成長 | **完成** | 規格 07；`docs/re/32` |
| 2.12 | 升級（門檻、階級、技能點） | **完成** | `docs/re/31`；入口在 RADIO（`docs/re/91`） |
| 2.13 | 地圖腳本 44 個 opcode | **完成（44/44）** | 有格子指到的一個都不缺（硬門檻 `missCells == 0`）；走不到的那 12 種逐支讀完（`docs/re/102`），最後一種 opcode 2 ＝ overlay slot 18 的**圖形對調**（`docs/re/104`）。未實作的統計從「16 種、85 筆」降到「**4 種、5 筆**」，而那四種全部是 section `0x10` 的索引越界（本來就該擋掉）——**門檻降到底了** |
| 2.14 | 條件閘（四個旗標） | **完成** | `docs/re/69` |
| 2.15 | 輻射與高溫 | **完成** | `docs/re/55`、`74`、`75` |
| 2.16 | 商店、醫生、訓練師 | **完成** | 規格 09、18；`docs/re/22`、`52`、`80`。折價指數 0 ＝ **原價**（`docs/re/22` §3 的比例表），不是全免 |
| 2.17 | 隊伍攻擊的命中基礎值查表 | **完成** | `ds:711Dh` ＝ **敵方這一回合的移動計畫**（16 格、一格一筆敵方記錄，`sub_14BF0` 每回合重排；`0xFF` ＝ 不動 → 50，會動 → 60，`docs/re/101`）。接成 `game.Battle.MovePlan` ＋ `game.HitBase`；remake 沒有敵人地圖移動，所以實際上一律 50（原本寫死 60）。門檻 `TestRoundUsesMovePlanAsHitBase`（拿掉接線會紅）|
| 2.18 | 角色記錄 `+0x4B`／`+0x4C` 的 bit0 | **完成** | `+0x4B` ＝ 參與過摧毀 Base Cochise（結局設）、`+0x4C` ＝ 總部已經表揚過（`docs/re/96` §5）。`StoreTo` 只動 bit0 |
| 2.19 | 物品／技能欄 ＝ 固定 30 槽、0 ＝ 空 | **完成** | `docs/re/15`、`42` §3：賣掉是清成 0 不壓縮，槽號是 `+0x1F`／`+0x25` 的意義；門檻 `TestSoldSlotSurvivesReload` |
| 2.20 | 店家庫存跟著存檔走 | **完成** | 物品表在存檔區、每槽一份（`docs/re/45` §2），賣一件 `+1`（`docs/re/42` §3）。`ItemTable.SetStock` ＋ `Rom.SetItemTable`，存檔時與存檔一起寫回；門檻 `TestShopStockSurvivesSave` |

## 3. 呈現層

| # | 工項 | 狀態 | 依據／證據 |
|---|---|---|---|
| 3.1 | 畫面佈局、外框、時鐘 | **完成** | 規格 03；`docs/re/25`、`27` |
| 3.2 | 地圖繪製與疊圖 | **完成** | `docs/re/84` 的覆蓋率門檻（42 張地圖） |
| 3.3 | 訊息視窗與排版 | **完成** | 規格 03、10 |
| 3.4 | 戰鬥畫面（隊伍名單） | **完成** | 規格 16；`docs/re/40`。**六欄都接上了**（`docs/re/103`）：`AMM` ＝ 裝備武器剩幾發（三道閘：沒裝備／bit7 設／近戰類別都印 0）、`WEAPON` ＝ 武器名。⚠ `+0x1F` 是 **1 起算**。資料行的狀態字與武器名走中文（`ui:wound.*` ＋ 物品名），查不到就整行退回英文。**版面照原版**（`docs/re/105`）：指令選單畫在欄 15–38、列 1–13 的面板上、一行一個選項；一行 **39 欄**、行首是序號 ＋ `>`；名單排到列 23（整隊都看得到）；戰鬥時指令列照留。⚠ **缺肖像框**（沒有敵人時原版畫遊俠）|
| 3.5 | 設施畫面 | **完成** | 規格 23；`docs/re/79` 的覆蓋率（23 家全非空）。進場就印主選單（`docs/re/42` §1），清單列物品名與技能名不列編號（`docs/re/17` §4）。病名走字串表 8 的第 1–8 條（`diseaseLabel`），查不到才退回 `status bit N` |
| 3.6 | 敵人肖像（`ALLPICS`，遭遇時） | **完成** | 遭遇時取第一隻敵人的 `+0x07` 畫在 (8,8) 96×84（與設施圖同一支載入器，位置相同）；門檻 `TestEncounterShowsPortrait`、`TestAllPicturesLoaded`（82 張全載得到） |
| 3.7 | 指令列 | **完成** | `docs/re/91`；畫在字元列 24（位置未 RE，重製決策） |
| 3.8 | 中文點陣字（倚天） | **完成** | 規格 10；UI 走 **960×600** 的 `HiFrame`（原版的 3×）。中文 **24×24**（`STDFONT.24`，光碟上是 ETUNPACK 壓縮的 `STD.24M`，用 `tools/etunpack.py` 解）、英數 **16×24**（`ASCFONT.24`，倚天自己的半形字）。只有 15 點字型時照原尺寸畫在格子中央。門檻 `TestCJKPathRendersPixels` |

## 4. 地圖指令列（七項）

入口全部定位在 `ds:AB1Ch`（`docs/re/91` §1）。

| # | 指令 | 狀態 | 缺什麼 |
|---|---|---|---|
| 4.1 | `Save` | **完成** | 真的寫回資料檔（`Rom.WriteSave` ＋ `cmd/wasteland -save-dir`），重開接得上（`TestSaveCommandWritesToDisk`）。**Y／N 確認已接**（`sub_19B4F`，見 4.9）。**寫回哪一份、序號怎麼推進未 RE**（`docs/re/97` §3.4），現在寫回讀進來的那一份 |
| 4.2 | `Radio` | **完成** | 兩輪都接上：第一輪表揚（賀詞在**階級表** `ds:D622h` 第 `0x3D` 條）、第二輪升級。**Y／N 確認已接**（見 4.9）|
| 4.3 | `Use` | **完成** | 三層選單（挑人 → S/I/A → 選項目）接上規則層 `Party.UseGate`；一個人時不問（照 `0x13A89`）。判定照原版。**清單分頁與原版同一套**（`docs/re/92` §3.2 把 `sub_198F0` 讀完之後對上 `docs/re/53` 的清單框架）：一頁九項、`1`–`9` 選這一頁的第幾項、`I`／`K` 翻頁且**翻到底不繞回**。`0` 那個鍵是重製版自己加的，給滑鼠用，它會繞回第一頁。⚠ 頁碼用中文數字：滑鼠是「點到哪一格送那一格的字元」，阿拉伯數字頁碼會被當成選項 |
| 4.4 | `Enc` | **完成** | `0x11CE7` 是 `sub_11CD0` 的**中途入口**——與自動遭遇同一支驅動器（`docs/re/94`）。`cmdEnc` → `StartEncounter`。**兩條問句都接了**：別組不在這張地圖（字串 `0x36`）、以及**這一組沒有敵人在打**（字串 `0x14`，答 Y 照樣進指令階段——那是原版讓玩家在空地上換武器／裝填／用道具的正規入口，`docs/re/105` §1）|
| 4.5 | `Order`（隊形） | **完成** | 抄到暫存 → 列出還沒放回去的人 → 逐一問「下一個是誰」→ 放回槽表；已放過的格子選不了、ESC 取消整份還原（`TestOrderReordersParty` 等三道）|
| 4.6 | `Disband` | **完成** | 選一個人分出去自成一組，站在原地；一個人不能分、滿四組不能再分（`TestDisbandThenView` 等三道）|
| 4.7 | `View` | **完成** | 切到下一組有人的隊伍（換人＋換座標＋換地圖）；繞回起點就什麼都不做 |

| 4.8 | **多隊伍（最多四組）** | **完成** | 存檔的四組槽表各自帶座標與地圖；`SwitchGroup` 切組前先把座標寫回槽表。**遭遇掃描四組都算**：只有同一張地圖的組參與，距離用各組自己的座標（`TestScanUsesEachGroupOwnPosition`）|
| 4.9 | `Save`／`Radio` 的 Y／N 確認 | **完成** | `sub_19B4F`（`docs/re/91` §2）：兩個指令走同一套，只有訊息編號不同（`Save` 是執行檔字串 `0x49`、`Radio` 是第 1 條）。`internal/play/confirm.go`；確認畫面吃掉其餘按鍵、ESC 與 `N` 都是取消（規格 27 §1）。門檻 `TestSaveAndRadioAskFirst`、`TestRadioCancelDoesNotConsumePraise`、`TestConfirmEscapeCancels`、`TestConfirmSwallowsMovement`。⚠ **兩個要一起接**：只接一邊的話另一邊仍然是按下去就發生，而那正是最容易誤觸的兩個（蓋存檔／用掉表揚）|

## 5. 遊戲外殼與流程

| # | 工項 | 狀態 | 依據／證據 |
|---|---|---|---|
| 5.1 | 從出廠存檔開始玩 | **完成** | `cmd/wasteland -mode play` |
| 5.2 | 標題畫面 | **完成** | `-mode play` 從標題畫面開場，按 `S` 進地圖（`-skip-title` 跳過）|
| 5.3 | 主選單 | **完成** | **原版沒有「新遊戲／讀檔」**——`sub_1630C` 的標籤字串只有 `Start`，第二支處理程式是一支 `retn`（`docs/re/95`）。存檔就是 `GAME1`／`GAME2` 本身；角色增刪在 Ranger Center |
| 5.4 | 角色建立 | **完成** | Ranger Center 的 `CREATE`／`DELETE`／`PLAY`，入口接在 `EnterFacility`（`docs/re/72` §3）；名字**可以打中文**（Big5，13 bytes ＝ 6 個字），建完寫進空的角色記錄槽並加進隊伍槽表 |
| 5.5 | 結局 | **完成** | 播放全解並實作（`docs/re/96`），**觸發點也解了**（`docs/re/100`）：資料裡沒有第 4 格是設計——`sub_1CB30`（主迴圈 `0x16C28`）在科奇斯基地自毀倒數 240 刻到期時合成 `al ← 84h`。倒數由腳本 opcode 35（資源 20 記錄 4）啟動。`TestCochiseEndgame` 從四把鑰匙走到結局
| 5.7 | 全隊陣亡的處置 | **完成** | 規格 28。`game.Party.Wipe` 三分支（什麼都不做／自動 `View`／死亡畫面）＋ `internal/play/wipe.go`：圖 0x3B、地點名與訊息**從映像直讀**（那兩段是明文 ASCII，不在字串表裡）。按鍵回標題是重製決策——原版沒有遊戲內重來（`docs/re/99` §1）。**「救得回來」的邊界已對完**：CON ∈ [−10, −1] 且八個狀態位元全 0（`sub_19A1D` 逐值對照，`docs/re/15` §3）；門檻 `TestWipeSalvageableBoundary` |
| 5.6 | 存讀檔選單 | **不做** | **原版沒有**——主選單只有 `Start`，存檔就是 `GAME1`／`GAME2` 本身（`docs/re/95` §4）。指令列的 `Save` 就是全部 |
| 5.8 | 功能鍵與面板（F1／F2／F5／F9／F10） | **完成** | 規格 27。**原版都沒有**：ESC 只取消不離開、F10 先問再先存後退、F5／F9 走獨立的 `WLQS` 快速存檔（不碰玩家的原版資料）。門檻 `TestQuickSaveRoundTrip`、`TestF10SavesBeforeQuitting`、`TestEscapeNeverQuits`、`TestSettingsTogglesMusic` |
| 5.9 | 背景音樂 | **完成** | 規格 27 §3。**原版沒有 BGM**（九首 PC 喇叭音效，`docs/re/44`），**十首**曲子是重製版自己寫的，`tools/make_music.py` 譜 → `tools/render_music.sh` 用 Roland MT-32／CM-32L 算成 ogg。曲子跟著場景與晝夜走（`internal/play/music.go`）。節奏鍵位是量出來的（`docs/mt32-rhythm-probe.md`）。ROM 與 ogg 都不入版控，`-music` 讀不到就沒有音樂、遊戲照跑 |
| 5.10 | 交付物打包 | **完成** | `tools/dist.sh` → `dist-all/`：`release/`（可散布，不含原版衍生素材，附 `setup.sh` 讓玩家自己產合成映像）、`local/`（本機完整包）、`promo/`（推廣片）。腳本會**檢查** release 裡沒有混進不可散布的檔，混到就中止 |

## 6. 中文化

| # | 工項 | 狀態 | 依據／證據 |
|---|---|---|---|
| 6.1 | 可翻字串（文本） | **完成** | 4,806／4,806（21 個不可翻的槽有清單）；地圖敘述在畫面上真的看得到（`TestWalkShowsTranslatedMessage`）|
| 6.2 | 段落書 162 段 | **完成** | `translations/zh-Hant/paragraphs/` |
| 6.3 | 遊戲內手札 | **完成** | 按 `P` 開，方向鍵翻頁，陷阱段落標 `[decoy]`；讀到編號直接顯示正文（162／162 有中文） |
| 6.4 | 中文說明書逐頁轉錄 | **完成** | `docs/manual-cht/` 八章 ＋ 1990 年譯名對照；33 張掃描全部有對應（封面、48 頁內頁、封底廣告、CD 盒、IBM 補充 6 頁、磁片標籤），讀不清的標 `〔字跡不清〕` |
| 6.5 | 官方英文手冊 markdown ＋ 繁中 | **完成** | `docs/manual/` 七章**中英對照**：英文照原樣（含錯字），中文接在每段之後或另立一欄。技能與武器譯名對過遊戲內字串表 |
| 6.6 | 攻略 | **完成** | **自建**，不是翻譯來的：`cmd/wl-atlas` 從 `game1`／`game2` 倒出 42 張地圖的設施、鎖、密語、傳送，`tools/summarize_walkthrough.py` 整理成表，正文按地區分八章（`docs/walkthrough/`）|
| 6.7 | 譯名表 | **完成** | `translations/glossary.md` |
| 6.9 | 中文角色名字 | **完成** | `lang.ToBig5`／`TextEntry.KeyRune`；`input.Input` 加 `Runes` 保留 IME 提交的完整字元（`Char` 是單一 byte，中文進不去）|
| 6.10 | 中文**接線**：畫面上真的是中文 | **完成** | 三層都接上（`docs/re/98` §2）：控制碼解碼（`textlayout.RenderBytes`）、表 1 以外的字串表（`exeStringN`）、`ui:` 37 條。戰鬥全程、設施選單與清單、角色管理、USE 結果、指令列訊息都走目錄。門檻 `TestPlayerMessagesAreTranslated`（六條玩家動作）、`TestCatalogueRendersWithoutControlCodes`（4,817 條解完不留控制碼）|
| 6.11 | 戰鬥的指令選單 | **完成** | 以前根本沒畫（`Prompt`／`CommandMenu` 只有測試在用），玩家不知道能按什麼。字串 55，七個選項排成一行（原版要九行、訊息視窗六行，怎麼容納還沒 RE，`docs/re/40` §5）|

## 6.8 展示

| # | 工項 | 狀態 | 依據／證據 |
|---|---|---|---|
| 6.8 | README 的遊戲畫面 | **完成** | `docs/images/`：地圖、中文訊息、手札、USE 選單。`wl-shot` 支援 `-lang`／`-font`／`-keys`／`-map`／`-title`／`-fn`／`-poll`，無頭產生 640×400 中文畫面 |
| 6.12 | 推廣片 | **完成** | 76 秒、1280×720、十四段（`docs/promo-video.md`）。三段管線全部可重跑：`tools/promo/shots.sh`（截圖）→ `tools/render_music.sh`（配樂）→ `tools/promo.sh`（合成）。**成品不進版控**，與原版資料同一個政策 |

## 7. 驗收

| # | 工項 | 狀態 | 依據／證據 |
|---|---|---|---|
| 7.1 | 子系統覆蓋率門檻（7 把尺） | **完成** | 腳本、遭遇、設施、戰鬥、存檔、翻譯、畫面 |
| 7.2 | 對原版 DOSBox 實機對拍 | **部分** | `docs/re/47`、`59`、`62`、`103`、`105`、`117`、`118`；移動、畫面、**名單六欄**、**戰鬥的四塊版面**、**設施買賣**都對過（對出九個既有的錯：`Knife` → `Kni`、行寬 38 → 39、行首缺序號、名單只排三個人、`ENC` 少一整條路、存檔沒寫目前地圖、設施版面整個不同、店家庫存拿錯一組、`P` 是集中金錢不是換人）。**還沒對**：有敵人的完整戰鬥回合、醫生與訓練師 |
| 7.3 | 劇本觸發覆蓋 ＋ 抽樣 | **完成** | 42 張地圖 708 筆條件記錄、**2,140 條條件**：`USE` 碰得到 2,066 條、只能走上去觸發 74 條（型別 3／4／5／6／7）、**碰不到 0 條**。段落引用 83 條、82 個編號**全部查得到中文正文**（`TestStoryTriggerCoverage`／`TestStoryTriggersAreReachable`／`TestParagraphTriggersHaveText`） |
| 7.5 | 抽樣試玩 | **部分** | 第一輪七段走完並記錄（`docs/re/97`）：建角色、遇敵打完、商店買賣、`USE` 開閘、讀段落、存檔重開都通了；結局那一段卡在資料側沒有觸發點（5.5）。剩下的是**開視窗的人工試玩**——無頭腳本驗不到手感、音效與連續操作 |
| 7.4 | 接線對帳 | **完成** | `TestWiringStatus`／`TestPlaceholders`，雙向 |

---

## 剩下的工作（2026-08-16 補完 A0 之後重新盤點）

RE 與 remake 的主線都走完了。抽樣試玩（`docs/re/97`）列的 A0 五項已經處理完
（`docs/re/98`）：三項補上、一項確認不是缺口、一項縮到一個具體的下一步。

### A0. 抽樣試玩找出來的五項——現在剩一項

| # | 項目 | 狀況 |
|---|---|---|
| 6.10 | 畫面上還有一大片英文 | **補完**。三層都接上（`docs/re/98` §2） |
| 2.20 | 店家庫存不進存檔 | **補完**。物品表跟著存檔寫回（`docs/re/98` §3） |
| —— | 醫生的治病清單印 `status bit N` | **補完**。病名是字串表 8 的第 1–8 條 |
| 5.7 | 全隊倒下之後遊戲照走 | **補完**。規格 28 ＋ `internal/play/wipe.go`：三分支（什麼都不做／自動 `View`／死亡畫面）都接上；地點名與訊息從映像直讀（那兩段是明文 ASCII，不在打包字串表裡）|
| 5.5 | **結局的資料側觸發點** | **已解**（`docs/re/100`）：資料裡沒有第 4 格是設計。索引 4 由 `sub_1CB30`（主迴圈 `0x16C28`）在自毀倒數到期時合成。三段接線一起補：`USE` 的收尾改寫、nibble 8 的呈現層、腳本 opcode 35 |

### A. 原版就沒有消費端的（解完了，接不上去因為沒地方接）

| # | 項目 | 狀況 |
|---|---|---|
| 1.14 | `CURS` 的八個滑鼠游標 | **已接**（規格 29、T10）：游標圖形用原版的，控制走重製版自己的路。**哪個圖形對應哪個狀態也接上了**（`docs/re/112`、T34）。⚠ 上一輪寫「全檔找不到讀 `CURS` 的程式碼」是**掃描器有洞**，不是真的沒人讀——負位移寫法漏掉了，而正對照當時同樣回零 |
| 1.15 | `TRANSTBL` 的 50 組對照表 | 形狀已解，三層掃描找不到讀它的人（`docs/re/56`）|

### B. 走不到的程式碼路徑（要靠改寫存檔才到得了）

| # | 項目 | 狀況 |
|---|---|---|
| —— | attract mode | **解完並接進遊戲**（`docs/re/113`、T34）：第 0 張字串表的六頁，每頁 255 個計時器刻。⚠ 觸發不是逾時，是**按過非 `S` 的鍵**；槽 8 那句話寫了但沒有任何呼叫端 |

### C. 文件與驗收（做多少算多少，不擋程式）

| # | 項目 | 狀況 |
|---|---|---|
| 7.2 | DOSBox 實機對拍 | 移動、畫面、名單六欄、戰鬥版面、設施三種（商店 T43、醫生與訓練師 T44）都對過；**有敵人的完整回合**還沒 |
| 7.5 | 抽樣試玩 | 無頭七段走完（`docs/re/97`），剩開視窗的人工試玩 |

## TODO：下一個 session 從這裡接（2026-08-17 交接，第四版）

> 先讀 [`CONTEXT.md`](CONTEXT.md) §0（目標與節奏）、
> [`docs/re/00-master-index.md`](docs/re/00-master-index.md)（速查）、
> [`docs/re/00-wiring-status.md`](docs/re/00-wiring-status.md)（哪些結論接上了）、
> **[`docs/re/97`](docs/re/97-playtest-sampling.md)（抽樣試玩找到什麼）** 與
> **[`docs/re/98`](docs/re/98-a0-wiring.md)（那些缺口怎麼補的、剩下哪一個）**。
> `cmd/wasteland -mode play` 從標題**玩到結局**全程可跑，畫面上的字是中文。
> **沒有一項擋著把遊戲玩完了**。
>
> **T1–T33 全部完成**（T13–T29 在 2026-08-17、T30–T33 在 2026-08-18）。**要接的是本節末尾
> 「下一步的順序」那張表**，不是往下逐條讀 T1–T33——那些是已完成的紀錄，
> 留著是因為每一條都寫了「症狀為什麼看不出來」。

### T1 — 結局的觸發點（已完成，2026-08-16）

**答案在 [`docs/re/100`](docs/re/100-ending-trigger.md)**：資料裡沒有第 4 格
不是漏掃，是設計。索引 4 由主迴圈的 `sub_1CB30`（`0x16C28` 呼叫，一輪一次）
在科奇斯基地的自毀倒數走完 240 刻時**自己合成**（`al ← 84h`）。
倒數由腳本 opcode 35 啟動，全 42 張地圖只有資源 20 記錄 4 用它。

要玩得到結局，三段接線缺一不可，而三段以前都是斷的：

| 斷點 | 症狀 |
|---|---|
| `USE` 成功之後沒改寫地圖格 | 圓柱插了鑰匙說 `It works!`，下一根永遠不出現 |
| nibble 8 沒有呈現層 | 踩上控制面板只印 `CHOOSE.`，答不了題 |
| opcode 35 沒實作 | 踩上去回報「還沒做」，倒數不會開始 |

驗收：`TestCochiseEndgame` 從四把鑰匙一路走到結局（第 240 刻）。
`TestEndingHasNoTriggerInData`（負面）與 `TestSelfDestructScriptIsUniqueInData`
（正面）成對守著。

⚠ 資料裡有兩筆記錄的跳表索引是 **92**，而表只有 0–48。
**它們走不到**——那張地圖零格 nibble 6、53 筆批次改寫沒有一筆寫成 6、
`sub_17CB1` 的靜態呼叫端也沒有 6（`docs/re/98` §5.1，`TestUnreachableJumpIndex` 守著）。

### T2 — 戰鬥與設施的 DOSBox 實機對拍（`WORKLIST` 7.2，戰鬥版面已完成 2026-08-17）

移動與畫面早就對過（`docs/re/47`、`59`、`62`）。這一輪補上**戰鬥的四塊版面**
（`docs/re/103`、`105`），對出五個既有的錯。**還沒對的剩兩塊**：
有敵人的完整回合（訊息序列）。設施買賣在 T43 對完，肖像框畫誰在 T41 解完。
⚠ 長 timeline 每個按鍵後要補 `key:Return`（`docs/re/68` §3.1）。

### T3 — 地點名 `Ranger Ctr.`（已完成，2026-08-16）

走**顯示時查表**（`translations/zh-Hant/places.tsv`，23 條招牌），
一個 byte 都不碰玩家存檔。資料裡的招牌是補過空白的定長欄位
（`"Ranger Ctr. "`），查表前要 trim。兩條測試雙向守著漏翻與多寫。

### T4 — 三個「原版就沒有消費端」的（解完了，接不上去）

| 項目 | 狀況 |
|---|---|
| `TRANSTBL` 50 組對照表 | 形狀已解；**沒有人讀它**是三層排除掃出來的結論（`docs/re/56` §3）|
| attract mode | **已經移出這一區**：內容解完並接進遊戲了（`docs/re/113`、T34）|
| —— | （`CURS` 已經移出這一區：滑鼠接上了，見 1.14）|

### T5 — 走不到的程式碼路徑

**做完了**（2026-08-17，`docs/re/102`）。12 種真正的 opcode 逐支讀完、補上 11 種；
最後一種 opcode 2 解在 `docs/re/104`——**44 種全部實作完**。
驗法是**自己造記錄**——0 格的路徑只有這一條路到得了，
而造出來的期望值一律來自組語不是實作（`internal/game/script_ops_test.go`）。

### T6 — 文件（`WORKLIST` 6.4–6.6）

**做完了**（2026-08-16）。三份都在版控裡：`docs/manual-cht/`（中文說明書逐頁轉錄）、
`docs/manual/`（官方英文手冊中英對照）、`docs/walkthrough/`（攻略）。

### T7 — 功能鍵、快速存檔、背景音樂（已完成，2026-08-16）

使用者要求的五件事都接上了，規格寫在 [`docs/spec/27`](docs/spec/27-remake-ui-additions.md)：
F1 說明、F2 設定、F5／F9 快速存讀檔、F10 離開（先問再先存後退）、ESC 一律只取消。

音樂那一項先確認了**原版沒有 BGM**（九首 PC 喇叭音效，只有音效 4 是 1.8 秒的旋律，
`docs/re/44`），所以不是「找不到原版音樂」而是「原版沒有這個東西」，
曲子由重製版自己寫。音源選 Roland MT-32／CM-32L：1988 年 DOS 遊戲的高階音源，
年代與機種對得上，比自寫合成器接近當年的聽感。

⚠ **ROM 與算出來的 ogg 都不入版控**，與倚天字型、原版資料同一個政策。
要有音樂就自己跑 `tools/render_music.sh`（需要 `docker/media.Dockerfile` 建的
`wasteland-media` image 與自備的 MT-32 ROM）。

### T8 — 推廣片（已完成，2026-08-16）

76 秒、1280 × 720、十四段，做法與坑寫在 [`docs/promo-video.md`](docs/promo-video.md)。
色票是從遊戲自己的畫面量出來的（`-colors 8` 直方圖），不是憑喜好挑的。

⚠ **成品不入版控**（畫面是原版的、聲音是 MT-32 算的）。腳本進版控，
`workplace/promo/` 整個 gitignore。要重出一版就照那份文件跑三支腳本。

這一輪順手補到 `wl-shot` 的：`-title`（停在標題畫面）、`-fn`（送功能鍵）、
`-poll`（推進亂數）。

### T9 — 解析度與英數對齊（已完成，2026-08-16）

畫布 640×400 → **960×600**，中文 16×15 → **24×24**，英數改用倚天自己的半形
`ASCFONT.24`（16×24）。三件事一起做才有意義：只換中文字級，英數還是
8×8 放大的話，混排會看得出兩套字型。

⚠ **英數不要拿遊戲原版的 8×8 字模放大。** 放大三倍之後筆劃有三像素寬，
擺在 24 點的中文旁邊像另一種字型。倚天附了同高度的半形 ASCII，用它。

⚠ 代價寫在規格 10 §2：半形字 16 寬、格子 24 寬，英文字母之間會多 8 像素空隙。
要讓英文密排就得讓英數用不同的推進量，那會破壞表格欄位對齊（戰鬥名單靠它）——
**格子對齊優先於英文密度**。

### T10 — 滑鼠（已完成，2026-08-16）

規格 29。**控制走 remake 自己的路**（使用者定案 2026-08-16：功能等價就好）：
原版是「座標比對 21 筆熱區表 → 換成按鍵碼 → 走鍵盤路徑」（`docs/re/43`），
那一套的每一格都綁在原版的畫面座標與遮罩狀態上，而這裡的畫面已經
960 × 600、訊息視窗也重排過。照抄會把一堆耦合搬進來，換到的功能一樣。

重製版的規則只有兩條：**點到哪一格就送那一格上的字元**（指令列與清單共用，
所以中文版也對——熱鍵字母本來就是 ASCII）、**點地圖就往那個方向走一步**
（絕對值大的軸決定方向）。右鍵 ＝ 取消。

⚠ **滑鼠不是新的一條輸入路徑**：點擊翻成與鍵盤等價的 `input.Input` 再走
同一條 `Update`。多一條平行分支就會有「鍵盤做得到、滑鼠做不到」的漏洞，
而那種漏洞每加一個模式就多一個。門檻 `TestMouseCommandBarMatchesKeyboard`
直接比對「同一個動作用鍵盤與滑鼠，結果狀態相同」。

⚠ **繪製與命中判定共用同一支逐格走訪**（`eachCell`／`eachMessageCell`）。
兩邊各走一次遲早會漂成「看到的字」與「點到的字」不一致，而那種錯沒有症狀。

游標用原版的 `CURS`（美術資產，`docs/re/57`）。**哪個圖形對應哪個狀態
在 T34 解完並接上**（`docs/re/112`）：編號的語意照原版，熱區是重製版自己的。

### T11 — `USE` 清單分頁（已完成，2026-08-16）

清單超過九項時第 10 項以後選不到——這是唯一一項玩家真的會咬到的介面缺口
（技能學多了就會遇到）。一頁九項、`0` 或上下鍵翻頁、繞回第一頁。

分頁與原版同一套（`docs/re/92` §3.2 把 `sub_198F0` 讀完之後對上
`docs/re/53` 的清單框架）：一頁九項、`1`–`9` 選這一頁的第幾項、
`I`／`K` 翻頁而且**翻到底不繞回**。T34 把翻頁鍵補齊；
`0` 那個鍵是重製版自己加的，給滑鼠用，它才會繞回第一頁。

⚠ **頁碼用中文數字**（「（第二頁／共三頁）」）。滑鼠的規則是
「點到哪一格就送那一格的字元」，阿拉伯數字的頁碼會被當成「選第 N 項」——
`TestUsePageLabelHasNoDigits` 守著。反過來翻頁鍵 `0` 是 ASCII，
所以**滑鼠點它自動就能翻頁**，不必為滑鼠多寫一條路。

### T12 — `Save`／`Radio` 的 Y／N 確認（已完成，2026-08-16）

`sub_19B4F`（`docs/re/91` §2）。兩個指令走的是**同一套**：印一句訊息 →
等 Y／N → 答 N 就走人，只有訊息編號不同（`Save` 是執行檔字串 `0x49`、
`Radio` 是第 1 條）。實作在 `internal/play/confirm.go`。

⚠ **兩個要一起接。** 只接一邊的話另一邊仍然是「按下去就發生」，
而那正是玩家最容易誤觸的兩個——`Save` 會蓋掉存檔、`Radio` 會用掉表揚。

確認畫面**吃掉其餘所有按鍵**（等 Y／N 的時候按方向鍵不該走路），
ESC 與 `N` 都是取消（規格 27 §1：ESC 一律只取消）。

⚠ **接上去會弄紅一批既有測試**——它們都假設按下 `S`／`R` 就發生。
測試改成走完整條路（`confirmed(t, s, key)` 這支輔助），**不要改成直接叫
`doSave`／`doRadio`**：那樣測得到效果，卻測不到「按鍵真的接到那個效果」。
唯一的例外是 `ending_test.go`——`BeginEnding` 之後場景不在地圖上，
按鍵那條路進不來。

### T13 — 命中基礎值的那張表（已完成，2026-08-17）

**答案在 [`docs/re/101`](docs/re/101-enemy-move-plan-table.md)**：`ds:711Dh` 是
**敵方這一回合的移動計畫**，16 格、一格對一筆敵方記錄，`sub_14BF0` 每個戰鬥回合
先全部清成 `0xFF` 再逐筆重排。值的版面是「高 2 位 ＝ 三句訊息的哪一句
（換位置／逃跑／逼近）、低 6 位 ＝ 3 × 3 鄰域的步向」。

命中基礎值因此的意思是：**這一回合會移動的敵人比較好打（60），
留在原地的比較難打（50）**。方向不要記反——隊伍那條是 `roll < 累加值` 才命中。

⚠ **remake 因此改成 50。** 沒有實作敵人地圖移動（`docs/re/87` §2），
所以每一格都是 `0xFF`。接法是把計畫放進 `game.Battle.MovePlan`
（`NoMovePlan` ＝ `-1`）、基礎值由 `game.HitBase(plan)` 算，
**不是把常數從 60 改成 50**——這樣移動實作出來的那天 60 那一支會自己亮起來。
這是一個玩得出來的差異：命中率整體降 10 個百分點。

⚠ `MovePlan` 的零值 `0` 是一個**合法的步向**。用結構字面值建 `Battle`
會安靜地變成 60，所以一律走 `NewBattle`（`TestNewBattleHasNoMovePlan` 守著）。

解法：一次範圍掃描（`export_range_refs.py 0x711D 0x7200`）找出全檔只有四處碰它，
其中兩處是寫入、都在 `sub_14BF0`。**「表是執行期填的」不是終點，是下一個問題的開頭。**

### T14 — 傷勢等級逐值對照（已完成，2026-08-17）

`sub_19A1D` 的門檻表在 `ds:CCCDh`（`FF F5 EC E2 D8`），**第 0 格一次都沒被讀到**
（迴圈從 `dl = 4` 起、減到 0 就跳出）。逐值展開寫進
[`docs/re/15`](docs/re/15-character-record.md) §3：

| 等級 | CON | 狀態字 |
|---:|---|---|
| 5 | **恰為 0** | 骷髏字模 |
| 0 | −1 … −10 | `UNC` |
| 1 | −11 … −19 | `SER` |
| 2 | −20 … −29 | `CRT` |
| 3 | −30 … −39 | `MRT` |
| 4 | ≤ −40 | `COM` |

⚠ **等級 0 是昏迷不是「沒事」。** 這一支只在角色倒下時被呼叫，
CON 為正的人根本不查它。`WoundNames` 原本寫成「0 ＝ 空字串、5 ＝ UNC」，
於是**死掉的人在名單上顯示成昏迷、昏迷的人什麼都不顯示**——
兩個都不會讓任何斷言變紅，因為呈現層自己補了一層對照把等級 0 換成 `UNC`，
剛好蓋住其中一半。表對齊之後那層對照就不需要了。

⚠ **`0x7F` 不是 DEL，是骷髏字模**（原版主文字字型第 `0x7F` 格，`docs/re/17` §4.4）。
倚天半形字型那一格是別的圖形（實測 40 個亮點，原版那格 288 個），
所以 `drawASCII` 對 `0x7F` 一律走原版 8 × 8 字模。
`TestDeathGlyphComesFromOriginalFont` **比對兩份字模的亮點數**——
只驗「有沒有像素」的話兩條路都會綠。

順帶確認：`Party.Wipe` 的「救得回來」邊界（CON ∈ [−10, −1] 且狀態位元全 0）
與原版一致，`TestWipeSalvageableBoundary` 補上門檻。

### T15 — 走不到的那 12 個 opcode（已完成，2026-08-17）

**筆記在 [`docs/re/102`](docs/re/102-unreachable-opcodes.md)。** 12 種逐支讀完、
補上 11 種；未實作的統計從「16 種、85 筆」降到「5 種、11 筆」，
其中 4 種 5 筆是 section `0x10` 的索引越界（本來就該擋掉）。最後一種 opcode 2 隨後也解了（T17）。

補進來的三塊語意值得記：

- **opcode 0 比的是別支隊伍**（不是「目前地圖／座標」）：掃四組槽表，
  跳過自己這一組、跳過不同地圖的，再比對記錄 `+0x07` 起的座標清單。
  ⚠ **「跳過自己這一組」那一行不能省**——少了它條件恆真，
  而症狀看起來像資料的問題。
- **opcode 4／39 是一對**：寄放一個角色（副本清掉物品、裝備、護甲、金錢，
  **原本那個隊員不動**）、過了 `0x2D00` 刻再領回來。
  ⚠ opcode 39 的分支目標是 `+0x04`／`+0x06`／`+0x08`，**不是常見的 `+0x03`／`+0x05`**。
- **opcode 14 是 opcode 35 的另一半**：把自毀倒數印成 `mm:ss.ff` 然後停住。
  240 刻 ÷ 4 ＝ 60 秒，所以畫面上是 `1:00.00`。

⚠ **當時 opcode 2 留著不猜**（洞會被統計出來，猜錯的實作不會）。
下一輪查了 `docs/re/04` 的 slot 表就解開了——見 T17。

⚠ 規則層新增兩塊狀態（都不進存檔）：`World.Groups`／`MapID`／`GroupIndex`
（opcode 0 要）與 `World.Stash`（opcode 4／39 要）。前者由 `syncGroups`
在 `LoadMap` 與 `storeGroup` 兩處抄過去——**忘了抄是安靜的錯**。

### T16 — 名片行的 `AMM` 與 `WEAPON`（已完成，2026-08-17）

**筆記在 [`docs/re/103`](docs/re/103-roster-line-columns.md)。**
`docs/re/15` §4 早就把六個欄位的順序釘死了，但只有四欄寫進 remake——
**另外兩欄從頭到尾是空的，而所有欄位測試都只驗有接的那四欄**。

- `AMM` ＝ 裝備武器剩幾發，**三道閘**：沒有裝備／那一格的附屬 byte bit7 設起來／
  武器類別不在 `ds:CD00h` 那張表裡（近戰），任何一道不過就印 0。
  過了才印**低 6 位**。⚠ 最容易漏的是第三道——漏掉的話近戰武器會顯示一個
  合法但沒有意義的數字。
- `WEAPON` ＝ 裝備武器的名字。
- ⚠ **`+0x1F` 是 1 起算的**：`sub_19AC8` 算 `0xBB ＋ 2n`，而物品陣列從 `+0xBD` 起。
  當成 0 起算會整批差一格，症狀是「顯示的是背包裡的下一件」。

資料行的中文另外組（`rosterRowCJK`）：狀態字走 `ui:wound.*`、武器名走物品名。
**死亡那一格不進翻譯目錄**——它是字模不是文字。查不到翻譯就整行退回英文。

⚠ **新增 `ui:` 鍵要同時改三個地方**：`translations/source/ui.tsv`（原文）、
`translations/zh-Hant/ui.tsv`（譯文）、`lang_coverage_test.go` 的
`uiCatalogueKeys`。少最後一個的話那幾條會被算成「孤兒 key」而讓覆蓋率測試變紅。

⚠ `translations/source/` **現在進版控了**（使用者定案 2026-08-17，見下），
所以三個地方都在 repo 裡，改完 commit 就好。

### T17 — opcode 2（已完成，2026-08-17）

**筆記在 [`docs/re/104`](docs/re/104-opcode-2-icon-swap.md)。**
`sub_10036` 是 overlay 的第 18 格，做的是**把兩張圖形對調**：
`seg003:0x420 ＋ 編號 × 128` 的 128 bytes 互換（那是疊圖與圖磚**連續的同一張表**），
再把兩者之中編號 < 10 的那一張的遮罩與 `0xDF60` 對調——
**那 32 bytes 在映像裡全是 0**，換上去等於畫成不透明。

出貨資料六筆都是「隊伍的疊圖（編號 7）↔ 一張地形圖磚」，而且成對出現
（`07↔5F`、`5F↔07`…）。`xchg` 是對稱的，所以那是**變裝的開關**：
隊伍在地圖上變成一塊地形，再跑一次換回來。沒有格子指到，遊戲裡看不到。

⚠ **這一題的教訓不是逆向技巧，是查表。** `docs/re/34` 寫「呼叫 `sub_10036`」、
`docs/re/04` 寫「slot 18 交換兩張圖磚」，兩份都對，
中間差的只是 `(0x10036 − 0x10000) ÷ 3 ＝ 18` 這一步。
**下「語意未解」之前先 grep 那個位址**——它在別的筆記裡可能是一整列。

⚠ 順帶：`docs/re/04` slot 18 那一列原本漏了遮罩那一半，已補。

### T18 — 名單對原版實機截圖（已完成，2026-08-17）

`docs/re/47` §5 早就錄下原版的名單畫面，**而它正好是剛接的 `AMM`／`WEAPON`
兩欄的 oracle**。四行逐欄對過，對出一個**既有的**錯：

`Knife` 在 remake 顯示成 `Kni`。名字的單數形是**字根 ＋ 單數字尾**
（`Kni\nfe\nves\n` → `Knife`），而 `singular()` 只取了字根。
`itemName` 一直這樣，所以 USE 清單與商店也受影響。

⚠ **它躲過了所有測試**，因為出貨資料裡大多數名字的單數字尾是空的
（`Crowbar\n\ns\n`）——兩種讀法結果一樣。
**驗「切字尾」的規則要挑字尾非空的樣本**，不是挑最常見的。

⚠ 順帶修掉 `docs/re/28` 那張表裡的假例子（`hammer\ns\n\n` → `hammers`／`hammer`
把單複數寫反了；出貨資料裡沒有這個字串，真的是 `Sledge hammer\n\ns\n`）。

> **實機截圖一次驗四行 × 六欄，而抓到的那一欄的錯是別的地方留下來的。**
> 這就是對拍排在未解逆向前面的理由。

### T19 — 對拍抓出 `ENC` 少掉一整條路（已完成，2026-08-17）

**筆記在 [`docs/re/105`](docs/re/105-enc-empty-round-and-menu-region.md)。**
在原版按 `E`（空地上），畫面問的是：

```
This party is not being attacked. Do you want them to execute a battle round?
Yes / No
```

答 `Y` **照樣進戰鬥指令階段**。那是原版讓玩家換武器、裝填、用道具的正規入口——
那些都是戰鬥指令，地圖上沒有對應按鍵。

remake 原本印一句 `Nothing to fight here.`，而**那不是原版的字串**，
是重製版自己寫的。⚠ **一句「自己寫的提示」可以蓋掉一整條原版路徑**：
它站在字串 `0x14` 那條分支的位置上。`ui:` 前綴標得出「這是重製版寫的」，
但**每一條都該問一次「原版在這個位置說什麼」**。

順帶解掉 `docs/re/40` §5 開著的問題：**戰鬥的指令選單不用訊息視窗**，
畫在欄 15–38、列 1–13，左邊放肖像。`sub_19727` 的四個設定與截圖逐項對上。
那一塊 remake 還沒實作（列在「下一步的順序」）——但它**不再是「未 RE」**。

### T20 — 戰鬥版面照原版重做（已完成，2026-08-17）

`docs/re/105` §2 解出來之後就實作了：指令選單畫在**面板**
（欄 15–38、列 1–13，`Scene.msgRect` 在戰鬥時回的就是它），一行一個選項。

⚠ **繪製與滑鼠命中判定共用 `msgRect`**。分成兩份的話戰鬥中點選單會全部落空，
而畫面看起來完全正常——與 `eachCell`／`eachMessageCell` 同一條規矩。

接的時候順帶對出**三個原本就錯的地方**，三個都是實機截圖看出來的：

| 原本 | 正確 | 症狀為什麼看不出來 |
|---|---|---|
| 名單一行 38 欄 | **39 欄**（武器欄 `0x20` 起 ＋ 7 格）| `Crowbar` 切成 `Crowba`，像「名字比較長」|
| 行首沒有東西 | **序號 ＋ `>`**（`0x1709A` 印數字、`0x170A0` 印 `0x3E`）| 整行往左差兩格，每一欄自己還是對齊的 |
| 名單排到列 17 | **排到列 23**（戰鬥時訊息視窗那 6 列是空的）| **第四個隊員不見**，像「隊伍只有三個人」|

另外兩個是中文那條路自己的：`hiText` 一次只排一個人，所以 `Roster` 回的
`Index` 永遠是 1（整份名單印 `1>`）；Big5 那一行沒有截到 39 格，
長武器名衝出畫面。⚠ **截 Big5 不能用 `len`**——一個中文字兩個 byte 一格，
拿 byte 數截會切在字中間。

戰鬥時**指令列照留**（實機截圖），只有設施與結局蓋掉。

⚠ 還缺**肖像框**（欄 1–13、列 1–12）。要先答「畫的是誰」：沒有敵人時原版畫遊俠，
有敵人時是不是敵人肖像還沒對拍。

### T21 — 交付物重新產一輪（已完成，2026-08-17）

畫面改過之後 README 的截圖、推廣片與 `dist-all/` 全都對不上了，
而**三樣都不會讓任何測試變紅**。這一輪重產：

- `tools/readme_shots.sh`（新）：README 的九張截圖，**含一張戰鬥畫面**——
  以前沒有，而戰鬥正好是這一輪改最多的地方。
- 推廣片：截圖重產 ＋ 重算（76 秒）。順帶修掉字面值 `4,902 條譯文` → `4,910`
  （`make_promo.sh` 兩處、`dist.sh` 的 release README 一處）。
- `dist-all/`：release 18M、local 40M、music 16M、promo 3.3M，
  release 的散布檢查通過。

⚠ **截圖過期不會有症狀**：它只會讓文件描述一個已經不存在的畫面。
所以重跑的指令寫進「下一步的順序」下面，不要靠記憶。

### T22 — 文件對一遍現況（已完成，2026-08-17）

程式碼改了九輪，**四份索引型文件停在幾輪之前**，而一項都不會讓測試變紅。
逐項改掉：

| 文件 | 寫的 | 實際 |
|---|---|---|
| `CONTEXT.md` | 接線表 100 份、函式索引已分析 435 | **105 份**、**464**（重跑 `gen_func_index.py`）|
| `CONTEXT.md` | 指令列「remake 已接 `Save` 與 `Radio`，其餘五項只定位到入口」 | **七項全部接上**，兩項還各帶 Y／N 確認 |
| `CONTEXT.md` | 命中基礎值查表 `0x711D`「未解，用 60」 | 解了（`docs/re/101`），`0xFF` → 50 |
| `CONTEXT.md` | 中文排版 640 × 400、`STDFONT.15` | **960 × 600**、24 點優先 |
| `CONTEXT.md`／`README.md` | 譯文 4,854／4,806 條 | **4,910**（`build_lang.py` 的輸出是權威數字）|
| `docs/spec/00-index.md` | 八列「擋住它的東西」 | 那八件事後來都解了；「實作順序建議」讀起來像待辦，其實全做完 |
| `docs/re/83` | 「目錄長度也是 4,806」 | 目錄多了重製版自己的 104 條 `ui:`／`places:` |

還補上兩份索引漏掉的筆記：`CONTEXT.md` 少 15 份（88–105）、
`docs/re/00-master-index.md` §8 少 9 份（97–105）。

> **這類過期完全靜音，而且沒有一份是錯字。** 每一句寫下的當下都是對的，
> 是**別的檔案**後來改了。所以每一輪收尾要**回頭讀單一入口**，
> 不是只更新 `WORKLIST`——`WORKLIST` 是自己那一輪寫的，本來就不會過期。

### T23 — 面板文字滿了要捲動（已完成，2026-08-17）

**筆記在 [`docs/re/106`](docs/re/106-text-scroll.md)。**
戰鬥指令階段問第二個人時，他的選單只看得到三個；第三、四個人整段不見。

原版的答案不是「清掉重畫」也不是「切掉」——**它捲**。`sub_1A0C5` 在
「游標已經在區域最後一列」時把整塊往上搬一個字元列（overlay slot 10），
否則只把游標往下移。兩塊區域代進去都對得上（訊息視窗 23、戰鬥面板 13）。

順帶解出**文字捲動的速度系統**：`ds:465Bh` ＝ 等級 0–8（玩家按 `<`／`>` 調），
四張 9 筆表分別是「每步捲幾個像素列／步數／每步幾 tick／不捲時整行幾 tick」。

```
CD9B[n] × CDA4[n] ＝ 8          ← 每次換行剛好捲一個字元列
CDAD[n] × CDA4[n] ＝ CDB6[n]    ← 捲與不捲花一樣久，字出現的節奏不變
```

> **四張表分開看都只是九個數字，乘起來才說得出語意。**
> 單張表看不懂的時候找它的兄弟表。

⚠ 這一輪**推翻了兩份筆記的斷言**（正文已改寫成正確答案）：

| 原本寫的 | 正確 |
|---|---|
| `docs/re/04` slot 10 ＝「填矩形，填色取自 `ds:CD9Bh[ds:465Bh]`」 | 是**捲動**；那個值是**捲幾個像素列**，填色是 `sub_10879` 裡寫死的 0 |
| `docs/re/74`／`67`／`69`：`ds:465Bh` ＝ 時鐘的「時」，`sub_142ED` 暫換時間值 | `ds:465Bh` ＝ **捲動速度等級**；時鐘的時是 `ds:465Ah`，五個引用點沒有一個在這條路上 |

第二列連帶解掉一個掛著的未解：「`sub_1790B` 怎麼取那個被塞進時鐘的數字」
——**沒有數字被塞進時鐘**，那個問題不存在。

remake 這一側：`eachMessageCell` 先量最後一格落在哪一列，超出下緣就整段往上挪；
`DrawTextIn` 行數超過就丟掉最前面的行。**9 段速度與平滑像素捲動刻意不做**
（理由寫在 `docs/re/106` §7，是定案不是待辦）。

⚠ 寫測試時踩到一次**假綠**：`TestSecondMemberSeesTheWholeMenu` 原本找
第一個 `, choose:`，而面板上還留著**前一個人**的提示——那一份本來就完整，
所以有 bug 也綠。改成認「輪到的那個人的名字」才抓得到。
**驗「第 N 個」的時候，別用第一個找得到的東西當樣本。**

### T24 — 戰鬥指令有第二張跳表，動作在結算階段（已完成，2026-08-17）

**筆記在 [`docs/re/107`](docs/re/107-command-resolution.md)。**
盤點抓到「七個指令裡有四個按下去沒作用」。查下去發現不只是漏接——
**結算端從來沒有人讀過**。

`docs/re/41` §1 寫著「四支處理程式都不執行動作，動作在結算階段依
`ds:46D8h`／`ds:46DAh` 進行」，標的是**已確認**，但那句話是從
「處理程式不動作」**推**出來的，結算端一個位址都沒列。

結算端是 **`ds:A568h`**——與下令階段的 `ds:A43Bh` 同樣 10 筆、同樣用指令碼
當索引，兩張表在記憶體裡只差 `0x12D`。

| 指令 | 結算做什麼 |
|---|---|
| Weapon | `sub_1949E`：**切換**。選到已裝著的那格就卸下；類別 15 走護甲欄 `+0x25` 並設 AC |
| Load | 四道閘 → `sub_196DB`：附屬 byte ← `(舊值 & 0xC0) | 容量`，**彈匣整件消失** |
| Evade | 只印字串 82。下令階段也是空的——迴避真的只靠命中門檻那個 60 |
| Use／Hire | 入口定位到了，各有兩三支子函式沒讀 |

remake：`internal/game/resolve.go` ＋ `internal/play/round.go` 的 `resolveCommands`
（排在攻擊之後，照原版四趟的順序——**這一回合裝填的子彈這一回合用不到**）。
`W` 會開一份物品清單（呈現是重製版的，`sub_19394` 的回傳編碼未解；選完之後照原版）。
`H`／`U` 印一句 `ui:combat.notyet` 並重問——**不靜靜吃掉按鍵**。

⚠ 順帶抓到一個**正在影響戰鬥數值**的 bug：`weaponOf` 把 `EquipIndex` 當 0-based，
而名單那一欄的 `equippedSlot` 是 1-based。出廠的 Angela Deth 名單印 `VP91Z 9mm`，
**攻擊拿的卻是她的彈匣**（Dice ＝ 0，等於白打）。
`TestWeaponColumnAndDamageAgree` 守著兩邊算出同一件。

> **同一個欄位有兩個讀者、各自算索引，就會有一個是錯的。**
> 而錯的那個如果不在畫面上，永遠不會有人發現。

⚠ 這一輪學到的兩條：
- **「A 不做，所以 B 一定做」是推論。** 推論出來的結論要標推論，
  而且要列出「下一個入口」——這裡的入口只要 grep 一次 `ds:46D8h` 的讀取端。
- **一組跳表要問「有沒有第二張」。** 同一個索引在不同階段查不同的表很常見。

### T25 — 三句該是中文的話（已完成，2026-08-17）

使用者指出「`Hell Razor swaps equipment.`」與「`, choose:`」該是中文。查下來三件事：

| 問題 | 成因 |
|---|---|
| `，選擇：` 前面是半形 `,` | `exe:1:55` 的譯文開頭漏翻。全語料掃了一遍，**只有這一條**（另外六條含半形標點的是口徑 `7.62`、物理常數 `h=6.626`、人名 `Ken St. Andre`、與一句刻意保留的英文雙關）|
| 「這個指令還沒做。」不會出現 | `Choose` 只 append 到 `c.Log`，而畫面讀的是 `s.message`／`s.cjk`。**只進 Log 等於沒說**——玩家只看到選單又出現一次 |
| 換武器清單少了翻頁鍵 | 中文那條路只印了頁碼，沒印 `0 翻頁`（英文那條有）|

⚠ 前一則的英文截圖是我用 `-font /nonexistent -lang /nonexistent` 跑的**隔離測試**，
用來單獨驗英文那條路——貼出來時沒標明，看起來像遊戲真的沒翻譯。
**貼截圖要講清楚那是哪一條路跑出來的。**

⚠ 寫測試時第三次踩到假綠：`TestResolveMessagesHaveChinese` 原本用
`c.CJK == nil` 判斷「有沒有接目錄」——**沒載目錄時那仍然是一支非 nil 的函式**，
只是每次回 nil，所以測試永遠 skip 不掉也永遠綠。改成自己 `LoadCatalogue`。
**「東西在不在」與「東西有沒有內容」是兩個判準。**

順帶把 `docs/re/98` 從「不適用」改成「已接」：§2 的中文三層
（目錄有譯文 → 消費端查得到 → 控制碼解得開）現在有測試守著，
而覆蓋率測試只量得到第一層。

### T26 — 戰鬥 `Use`／`Hire` 的參數解了（已完成，2026-08-17）

**筆記在 [`docs/re/108`](docs/re/108-combat-use-and-hire.md)。**
順序表第 1 位那一項的 RE 部分。三個發現，各關掉一份筆記掛著的問題：

| 發現 | 關掉了 |
|---|---|
| `Use` 的指令參數 ＝ `(選項 << 4) \| 方向`；編號另存 `ds:A9FDh[角色編號]` | `docs/re/41` §7「`ds:A9FDh` 的欄位配置」——**它沒有欄位配置**，整個 byte 就是編號 |
| `sub_12738` ＝ `al << 4`（合成**高**半部）、`sub_17C69` ＝ `al >> 4` | 同上那一列的另一半 |
| 九向位移表 `ds:AAB1h`，第 5–8 格是四個對角增量 | `docs/re/26`「斜走的四個入口誰在用」 |
| `sub_13AE4` 的另一個呼叫端 `0x1240E` 就是戰鬥的 `Use` | `docs/re/92` §5 |

⚠ **對角那四格選不到。** `ds:AAB1h` 全檔只有一個讀取點、`sub_164E0` 只有一個
呼叫端，而那條路在 `0x1260E` 把索引 5–8 折成 0–3（方向鍵折進 `IJKL`）。
這個結論有**全檔字面值掃描**佐證：把四個位址當成 16-bit little-endian 掃整份映像，
每個都只在這張表自己裡面出現一次。

> **跳表裡的位址是資料不是呼叫，xref 圖上什麼都看不到。**
> 要說「這幾格沒有人用」，得掃字面值。

`Hire` 的參數是「遭遇佇列裡的第幾組」：`sub_13449` 取低 6 位、
`sub_13A56` 拆成 `mod 3`（子組）與 `div 3 & 3`（隊伍組）——
正是敵方記錄「3 組 × 10」與佇列「4 個隊伍組」的拆法。

當時**接線還沒做**：`Use` 要把「選項＋編號＋方向」接到 `Party.UseGate`、
`Hire` 缺 `0x132BF` 之後併隊伍的三支。兩條後來都補上了（T27、T32）。

⚠ 這一輪順手清掉三份筆記「還沒解」清單裡**已經解掉的項目**。
`docs/re/92` §5 列著三支目標判定未解，而**同一份的 §6 把三支都解完了**。
**解完一項就回頭刪那一列**——留著會讓下一輪重讀一次。

### T27 — `ORDER` 改走原版字串，戰鬥 `USE` 接上了（已完成，2026-08-17）

使用者回報「`Which group` 也要中文」並問「`USE` 是不是早就接上了」。逐項查：

**`USE` 的確接上了，但只有地圖那一半。**

| | 狀態 |
|---|---|
| 地圖指令列的 `USE` | **已接且已中文化**（`要哪一位？` → S／I／A → 清單）。規則層 `Party.UseGate`、呈現層 `internal/play/use.go` |
| 戰鬥指令的 `USE` | 當時**沒接**（參數格式那一輪才解出來，`docs/re/108` §1）。T27 接上了 |

**指令列七項逐一截圖看中文**，找到的英文只有一處——`ORDER` 的
`Who is next?`。而它是**重製版自己寫的字，站在原版字串的位置上**：
原版 `0x12B10` 印的是**字串 15**（`Pick a player:` → 目錄裡早就翻成
`選一位隊員：`）。收尾那句 `Order: A, B, C` 原版也沒有。

> **這是 `docs/re/105` §5 那條規則的第二個實例**：
> 一句自己寫的提示會蓋掉一條原版字串。`Nothing to fight here.` 蓋掉的是
> 字串 `0x14`，`Who is next?` 蓋掉的是字串 15。
> **每一句重製版自己寫的話都該問一次「原版在這個位置說什麼」。**

三項都做完了：

1. `ORDER` 改走**字串 15**，中英兩條路各一份；收尾那句（原版沒有）標成 `ui:order.done`。
2. **戰鬥 `USE` 接上了**：`U` → S／I／A → 清單 → **問方向** → 記成指令參數
   `(選項 << 4) | 方向`，編號另存一格；結算時對「往那個方向一格」的記錄跑
   `Party.UseOn`，收尾照原版改寫那一格（新增 `World.PatchCell`）。
   實跑驗過：`E U S 1 I A A A` → `Hell Razor 使用，成功了！`
3. `Hire` 當時仍未接（§3 那三支還沒讀）。那三支在 `docs/re/110` 讀完並接上了。

⚠ 接的過程踩到兩個坑，兩個都寫成測試守著：

- **UTF-8 漏進 Big5 串**：收尾那句的分隔符直接寫 `、`，三個 byte 被當成
  Big5 高位元組解讀，畫面上變成「Vargas粻 hrasher」。
  `TestCJKMessagesAreValidBig5` 掃整段輸出，兩兩配對之後不准剩落單的高位元組。
- **兩個建構點各自手接欄位**：`CombatScene` 在 `StartEncounter` 與
  `beginEmptyRound` 兩處建，我只在其中一處加了 `World`，於是走 `ENC` 那條路
  進戰鬥時 `USE` 什麼都不做——**畫面上完全正常**。
  已收攏成一支 `wireCombat`，測試直接檢查 `c.World` 非 nil。

> **同一個結構在兩個地方各自手接欄位，遲早會漏一個；而漏掉的症狀是
> 「那個功能安靜地不做事」。** 有兩個建構點就把它們收成一支。

### T28 — 每一幀的配置從 902 次降到 2 次（已完成，2026-08-17）

使用者問「GC 還會有壓力嗎」。量出來的答案分兩半，**而壓力不在被問的那一側**。

先建一支跨分支都能跑的基準（`internal/play/framebench_test.go`，
只碰兩個分支型別相同的 API），量「合成一幀」的配置：

| | 配置次數／幀 | 位元組／幀 |
|---|---:|---:|
| 改之前（`master` 與 `utf8-experiment` **完全相同**）| 902 | 622,589 |
| 改之後 | **2** | 582,244 |
| 加上上色送螢幕（`ToImage`，改之前）| 905 | 2,933,277 |
| 加上上色送螢幕（`WriteRGBA`，改之後）| **2** | 582,163 |

兩件事：

1. **UTF-8 那條路一次配置都沒多加。** 兩個分支的數字逐項相同，
   `CachedRuneToBig5` 是 map 查詢，零配置。
2. **壓力來自畫字那一層，`master` 上早就有。** `assets.rowsOf` 把每個字模
   攤成 `[][]bool`，一個 24 點的字就是 **25 次配置**（外層一個、每列一個），
   一幀滿版中文 ≈ 900 次；60 fps ＝ 每秒五萬四千次。
   再加上 `ToImage` 每幀重配 2.3 MB 的 RGBA，一秒 138 MB。

改法兩處，都不動畫面內容：

- `render.blit` 直接讀點陣（`assets.Glyph`／新增的 `ASCIIGlyph`），
  不再攤平。`rowsOf`／`GlyphRows` 留著給測試當 oracle。
- `HiFrame.WriteRGBA(dst)` 寫進呼叫端自己的緩衝區，`internal/ui` 拿同一塊重複用。
  `ToImage` 留給一次性用途（截圖工具）。

驗收（**「看起來沒壞」在這裡證明不了任何事**——位元序讀反是左右鏡像、
每列 byte 數算錯是逐列偏移，兩種都畫得出像字的東西）：

- `TestBlitMatchesRows`／`...ASCII` 逐像素比對兩條路，四個字 × 三個位置。
  把位元序故意改成 `1<<(x%8)` 驗過會紅。
- `TestDrawCJKDoesNotAllocate`：畫兩個字的配置次數必須是 0。
- `TestBlitRejectsShortGlyph`：點陣不足一個字就不畫，不畫半個。
- 截圖 `01-map`／`03-use` 與版控裡那張**逐位元組相同**；`08-combat` 與版控
  那張不同，但**改前改後兩張自己逐位元組相同**——差的是版控那張過期
  （早於 `，選擇：` 那個逗號修正），不是這次改的。

⚠ **剩下的 582 KB 是 `NewHiFrame` 每幀一張**，沒有跟著改：`HiFrame()` 回傳的
畫布被截圖工具與測試拿在手上，改成重複用就是把回傳值變成別名，
**「前後兩幀比對」那類測試會從此恆等**——那是假綠，比多配置一次貴。
要改得先給視窗那一層一個 `HiFrameInto(*HiFrame)` 的入口。

⚠ **時間沒有跨版本比較**：量的時候這台機器被別的專案佔著（load average 24、
14 核），同一份程式碼前後跑差三倍。配置次數是決定性的，時間不是——
**跑不出可比的時間就只報可比的數字**，不要把噪音寫成結論。

### T29 — 記錄尾巴那 51 bytes 沒有主人，階級欄比想像的大（已完成，2026-08-17）

使用者問「角色記錄的 `0x4D`–`0x7F` 是什麼用途」。答案：**沒有用途**
（[`docs/re/109`](docs/re/109-character-record-tail.md)），三層佐證：

| 層 | 結果 |
|---|---|
| 全檔 `mov bl, imm` 落在 `0x4D`–`0x7F` | **5 處，沒有一處是角色記錄**（兩處是敵方記錄的 `+0x5D`、兩處在合成 16-bit 值、一處是 `ds:46B0h` 的參數）|
| `inc bl` 迴圈的界 | 屬性到 `0x15`、技能到 `0xBC`／`0xBE`、物品到 `0xF9`；沒有一個走進那一段 |
| 出廠資料 | `game1`／`game2` 的 7 筆記錄，`0x39`–`0x7F` 全零 |

順帶解掉一個一直寫錯的欄位：**階級字串沒有長度上限**。寫入端 `0x1BB6C`
逐字元抄到 NUL 為止、沒有邊界檢查，所以欄位大小是被下一個已知欄位 `+0x4B`
擋出來的（25 bytes），不是 14。實際最長的 `Lieutenant Commander` 寫到 `+0x46`。

程式改了兩處：`recRankEnd = 0x4B`、`putRank` 抄到 NUL 就停（**不清尾巴**，
原版也不清，清了就不是 round-trip）。四條測試守著，其中
`TestRankFieldHoldsTheLongestRank` 驗過舊的 14 bytes 會紅。

⚠ **這塊空地可以拿來放重製版自己的東西**（例如完整的 UTF-8 名字，
就不需要 `WLNAMES` 側車檔了）。但要用之前得先做一個實驗：把資料寫進去、
**用原版 DOSBox 跑一輪再存檔**，確認它不會被清掉。原版沒有寫入點，
但建立角色時那筆記錄的內容從哪來還沒讀。

### T30 — 手札長段落讀得到開頭了，UTF-8 合回主線（已完成，2026-08-18）

重跑 README 截圖時看出手札標題被正文蓋掉，追下去是兩層問題：

1. **捲動的上緣算錯**：`eachMessageCell` 拿區域第一列當上緣，而呼叫端為了
   讓出標題會把起點推到第二列。正文一捲動就疊在標題上——
   **兩行疊著還是看得到字**，截圖看起來像「那行很擠」，不像壞掉。
2. **手札根本不該自動捲到底**。訊息視窗要「最後一行一定看得到」
   （`docs/re/106`），手札要「從頭讀到尾」。套錯的代價量過：
   **162 段裡 27 段放不下六列，最長 38 列**——那 27 段的開頭讀不到，
   而且沒有任何辦法捲回去。

定案的手勢（使用者 2026-08-18）：`I`／`K`（與 `←`／`→`）換段落，
`Page Up`／`Page Down` 在段落內捲動。規格寫進 `docs/spec/19` §4.1。

⚠ 順帶修掉一個「標題說謊」：舊標題寫著「I／K 翻頁」，而程式只看 `Dir`，
`IKJL` 根本沒有綁進 `input.Bindings`——**按 I 什麼都不會發生**。
`TestJournalIKChangeParagraph` 現在守著。

同一輪把 `utf8-experiment` 合回 master：整條管線 UTF-8、只在畫字那一刻轉 Big5、
名字 30 bytes（超出原版 13 bytes 的部分進側車檔 `WLNAMES`）。
衝突三處都是兩邊各自新增，兩邊都留。

### T31 — 推廣片重跑，結局那一頁中英疊在一起（已完成，2026-08-18）

推廣片重跑（11 張截圖 ＋ 76 秒影片，約七分鐘）。看新產出的 frame-62 才發現
**結局敘述把英文與中文兩份都印了**：`showEndingPage` 兩個欄位都設，
而 24 點的中文填滿整個字元格，緊貼的兩行看起來像疊在一起。

改法照專案既有那條規矩（`showCombatPrompt`、`sayPlace` 都是這樣）：
**有中文就把英文清掉**，查不到譯文時 fallback 不變。
`TestEndingPageDropsEnglishWhenTranslated` 走完整段結局，
任何一幀同時有中英文就紅（拿掉修正驗過會紅，41 幀）。

譯文條數對齊到 **4,915**（`build_lang.py` 的輸出是權威）：
`tools/promo/make_promo.sh` 兩處、`tools/dist.sh` 一處、`README.md` 兩處、
`docs/spec/00-index.md` 一處。合併 UTF-8 分支時多了一條 `ui:save.donenonames`。

> **產出物的數字要跟著 build 的輸出走，不要各自抄。** 這一輪四個檔案裡有
> 三種數字（4,806／4,910／4,914），全部是某一次 build 當下的快照。

### T32 — `Hire` 的結算與 `sub_19E2A` 都解完了（已完成，2026-08-18）

兩個掛在「下一步」表上的未解逆向，一輪解完，[`docs/re/110`](docs/re/110-hire-resolution.md)、
[`docs/re/111`](docs/re/111-roster-inverse-video.md)。

**`Hire`**：NPC 不是「招一個空殼再長大」——它本來就是一筆完整的 256 byte
角色記錄，放在地圖區塊的 **section 17**，雇用成功就是**整筆抄進隊伍的下一個記錄槽**。
成敗由「招募者的 (魅力＋智力)/2 ＋ 魅力 ＋ 等級 ＋ 骰」對「NPC 的同一組數字」決定，
**魅力進去兩次**——與官方手冊那句「你想雇用 NPC 時，他的反應也看這一項」對得起來。
7 人上限比對的是 `ds:4656h`，滿了印字串 95「名冊沒有空位了」。

**`sub_19E2A` 三行**：`ds:4678h ← 0xFF` ＝ **反白開**，而 `ds:4678h` 早在
`docs/re/14` 就解過了。所以武器附屬 byte 的 bit7（卡彈）不是「多做一件事」，
是**把武器名畫成反白**；同一支在 `0x17102` 也被叫一次——狀態位元非 0 的隊員反白。

順帶關掉兩個掛著的問題：
- `ds:4653h` 與 `ds:4656h` 的差別（`docs/spec/05` §5）：**前者是隊伍槽表的索引、
  後者是角色記錄的編號**。出廠都是 4 只因為那四個人剛好連號。
- `docs/re/103` §2 的「bit7 未解」那一段改寫成答案。

⚠ 當時兩份都登記成**未接**。`Hire` 的實作在同一天稍後補上（`internal/play/hire.go`），
名單的反白**還沒做**，留在「下一步的順序」第 3 列。

### T33 — 軟體世界攻略進版控，網站上線（已完成，2026-08-18）

1990 年前後《軟體世界》第 5 期的連載《沙漠傳奇——荒野遊俠的故事》（作者 Y.M.J.，
紙本 p.56–61）整理進 `docs/walkthrough/swm-005/`，**兩份並存**（使用者定案）：

| 版本 | 用途 |
|---|---|
| `raw/` 逐字 | 一字不漏、錯字照留（`Oneedles`／`Scorpition`／`Sever motor`／「幅射塵」），要引用原話看這份 |
| 上層 整理 | 路線、地圖標註表、道具表、密語，加 **31 條機制斷言**逐條標了該查哪份 `docs/re/` |

網站：`site/`（`build.py` 產生的靜態 HTML，CSS 內嵌、零外部請求），
發佈在 <https://wicanr2.github.io/wasteland_cht/>。

⚠ **發佈方式換過一次**：本來走 GitHub Actions，但這個帳號沒有 Actions 額度，
改成 `tools/publish_site.sh` 手動推 `gh-pages` 分支。不能直接把 Pages 指到 `site/`——
分支模式的資料夾只能選 `/` 或 `/docs`（API 給別的值回 422），而 `docs/` 是逆向筆記。
**代價：改完網頁要自己跑一次腳本**，不會自動觸發。

⚠ `CLAUDE.md` 寫著「repo 目前 private」，實際是 **public**，已改。這一行決定
「推上去」等不等於「公開發佈」，而 §7 的不散布清單建立在這個前提上。

### 這一輪新增、下一個 session 要知道的

- **找一張跳表的觸發點時，先問「這個索引還能從哪裡變出來」。** 結局的索引 4
  找了三輪都在資料側找，因為前兩個消費點都吃資料。第三個消費點根本不看資料——
  它自己造一個 `al ← 84h`。**掃常數的寫入點比掃資料快得多。**
- **opcode 覆蓋率的門檻已經降到底**：「4 種、5 筆」，而那四種全部是
  section `0x10` 的索引越界值。再往下只有一種可能——那四個值不再出現，
  而那代表 section `0x10` 的解讀改了（回頭看 `docs/re/71` §5.2），不是調數字。
- **nibble 8 的呈現層在 `internal/play/question.go`**（單鍵／打字兩種模式）。
  答案**不走翻譯目錄**——比對逐 byte 全等而輸入層是 ASCII。
- **`USE` 清單一頁九項**，`0` 翻頁（滑鼠點那個 `0` 也行——翻頁鍵是 ASCII，
  所以自動吃到「點到哪一格送那一格的字元」那條規則）。
  **數字鍵是這一頁的第幾項**，不是整份清單的第幾項；兩者混淆的話翻到第二頁
  按 1 會用到第一項，而且完全不會報錯。
- **`USE` 的收尾會改寫腳下那一格**（`Party.UseOn`）：成功用位移 4
  （記錄 `+0x00` 的 bit3 設起來時改由通過的那一條條件算）、
  沒有吻合的條件用位移 6 並套懲罰。
- **攻略是自建的，不是翻譯來的。** `cmd/wl-atlas` 把 42 張地圖裡玩家碰得到的東西
  倒成 JSON（設施招牌、條件閘、問答、傳送、藏東西的格），
  `tools/summarize_walkthrough.py` 整理成 `docs/walkthrough/generated/` 四份表，
  正文按地區分八章引用它們。**改攻略內容之前先想這句話是不是資料講的**——
  資料看不出來的東西不要寫進去。
- **`wl-atlas` 有兩個坑寫在程式碼註解裡**，兩個都會產生「看起來完全正常」的錯資料：
  - `SectionRecord` 回傳「從記錄起點到區塊尾」，記錄沒有長度欄位。條件串列讀到
    `0xFF` 才停，**沒有終止碼的記錄會一路吃進下一筆**——症狀是「這一格的條件跟
    隔壁那一格一模一樣」。`boundedRecord` 用同一個 section 裡下一個更大的指標當上界。
  - **nibble 5 與 8 不能無條件列舉 section 記錄**：section 邊界是靠「第一個非空指標」
    推出來的，沒有格子佐證時讀出來的是雜訊（資源 0 會生出一句地圖敘述配一串不成話
    的答案）。nibble 8 改用三道篩（答案清單有終止碼、題目編號在字串數之內、
    打字題至少一條答案打得出來）。
- **nibble 10 記錄 `+0x00` 的低 6 位是字串編號**（`sub_16B17`，`docs/re/60` §3.1）
  ——就是「進入石英城。」那一句。以前兩條路都印死的 `TELEPORT.`：
  會問「進新地點？」的格子由 `0x16AEA` 在問之前印，不問的由 `0x16A21` 印。
  `TestTeleportPrintsLocationName` 守著兩條路。

- **中文的三層**：目錄有譯文 → 消費端查得到 → **控制碼解得開**
  （`textlayout.RenderBytes`）。三層任何一層斷了，症狀都是「畫面上是英文」，
  而覆蓋率只量得到第一層。加新訊息時三層都要走。
- **字串表不只表 1**：商店 7、醫生 8、角色管理 3、訓練 6（`Scene.exeStringN`）。
  拿表 1 去查會安靜地取到別的句子。
- **一個中文字佔一格**（倚天 24 × 24 ＝ 3× 放大後的一個字元格）。
- **`wl-play` 是抽樣試玩的主力工具**，這一輪補了：`-poll N`（推進亂數，
  **不給就不准寫「沒遇到敵人」**）、`-lang`／`-font`（trace 把中文印在〔〕裡）、
  `-save-dir`／`-modified`（存檔寫出去再讀回來）。狀態列改用 `Scene.Mode()`，
  看得出「進了設施但選單沒開」這種接線斷掉的情況。
- **`Save` 要真的寫檔就得給 `-save-dir`**，而且**不能指到 `workplace/orig/wastland`**
  ——那份驗 SHA-256，寫過就開不起來。複製一份出來（例如 `workplace/savetest`），
  第二次開加 `-modified`。
- **接線測試要從玩家的第一個按鍵開始。** 這一輪六個缺口全部在有測試的子系統上，
  因為那些測試都從場景中間的函式進場（`docs/re/97` §5.2）。
- **`ui:` 那組 key**（`translations/{source,zh-Hant}/ui.tsv`）是重製版自己的
  介面文字——原版寫死成 ASCII 不走字串表。加一條要**同時改 tsv 與
  `internal/play/lang_coverage_test.go` 的 `uiCatalogueKeys`**，
  否則不是被當成孤兒就是漏檢。
- **熱鍵字母不跟著翻譯走**：譯文寫成「U使用」，比對用靜態字母。
- **文字層整層走高解**（`Scene.hiText()`）：有倚天半形 ASCII 時，時鐘、
  戰鬥名單、設施那幾行、訊息視窗的英文都由 `HiFrame` 畫。
  低解那張**不可以再畫同一批字**——先畫再蓋會留殘影。
  `Frame` 跳過的每一項，`drawHiTextLayer` 都要補回來，少一項就是「那行不見了」。
- `wl-shot` 新增 `-journal <頁>`、`-ending`、`-ending-ticks`。
- **ESC 不准變成離開遊戲，任何一層都不行**（規格 27 §1）。以前有兩個測試寫著
  「F10 應該離開」，那是把誤觸變成資料損失的寫法；現在 F10 是**開確認**，
  `TestEscapeNeverQuits` 與 `TestF10SavesBeforeQuitting` 成對守著。
- **快速存檔寫自己的 `WLQS` 檔，不碰玩家的原版目錄**（那份驗 SHA-256）。
  讀檔的三道拒絕裡最重要的是「明文長度不合就拒絕」——硬套會讓角色記錄整個錯位，
  症狀像解碼壞了而不像讀錯檔。
- **`resetModes` 裡的 `s.asking = input.DirNone` 不能省**：`input.Direction` 的
  零值是 `DirUp`，忘了寫的話讀完檔畫面會停在「進新地點？」等 Y／N。
- **MT-32 不是 GM**：MIDI channel 1 不用（八個聲部吃 2–9、節奏在 10），
  音色編號走 MT-32 內建表。放在 channel 1 會**安靜地**沒聲音。
- **音源的鍵位不要照別的標準抄，要量。** MT-32 的節奏鍵只有 49 個有指派，
  而 **GM 的腳踏鈸 42 不在裡面**——第一版的鼓組因此少了一整層，
  檔案長度、音量、格式全部正常。`tools/mt32_probe.py` 三分鐘量完
  （能量看有沒有指派、過零率看高低、尾巴分腳踏鈸與碎音鈸），
  結果在 `docs/mt32-rhythm-probe.md`，`make_music.py` 開跑前擋一次。
- **`volumedetect` 印在 info 層**：習慣性加 `-v error` 會把它一起壓掉，
  那時候音量欄是空的而指令仍然 exit 0——看起來像量不到，其實是自己關掉的。
- **音效與音樂共用同一個 `audio.Context`**（44100 Hz）。一個行程只能有一個，
  第二次用不同取樣率建會失敗。
- **`HiFrame` 的模式條件要與 `Frame` 對齊。** `Frame` 在標題那一支提早 return，
  `HiFrame` 卻是照旗標判斷——漏一個旗標就會把指令列畫到標題畫面上，
  而且**只有載了倚天字型才看得到**（低解那條路完全正常）。
  `TestTitleScreenHasNoCommandBar` 守著。
- **要接進 Big5 位元組串的字串只能是 ASCII。** 說明面板的「字母欄」寫中文
  會被當成 Big5 高位元組解讀，畫面上出現看得懂筆畫卻不成字的東西。
  同一個道理適用任何拼進 `s.cjk` 的 Go 字面值。
- **訊息視窗只有六行**（`docs/re/25` §2）。多寫的不會捲動也不會報錯，
  **會安靜地被切掉**——說明面板原本九行，畫面上只看得到前六行。

### T34 — 六個懸案一次解掉，兩個接進遊戲（已完成，2026-08-18）

「下一步」表上掛著六列未解逆向，這一輪全部關掉，
[`docs/re/112`](docs/re/112-mouse-cursor-and-hotzones.md)、
[`docs/re/113`](docs/re/113-attract-mode.md)是新的兩份，其餘四個更新既有筆記。

| 項目 | 結果 | 接了沒 |
|---|---|---|
| 游標八個圖形對應哪個狀態 | `ds:8DCDh` 是索引、`0x10D4D` 是繪製常式；0 預設、1 可點、2–5 上下左右、6 中央方框、**7 選不到** | **接了**（`cursorGlyph`）|
| 片頭播什麼 | 第 0 張字串表的六頁，每頁 255 個計時器刻；**槽 8 寫了沒有呼叫端** | **接了**（`internal/play/attract.go`）|
| `sub_198F0` 超過九項怎麼呈現 | USE 的技能清單，排版與按鍵全走 `docs/re/53` 的清單框架 → **翻頁** | **接了**（USE 清單改用 `I`／`K`）|
| 移動計畫三分支 | `sub_19D2F` ＝ 有射程的武器類別（2–13）、`loc_12AA2` ＝ 敵人種類、士氣門檻 ＝ 整組剩餘血量 < 一隻基礎血量的一半 | 沒接，要等地圖移動 |
| 跳表索引 92 | 兩筆都在 `game1` 區塊 34，三道掃描證明走不到；真的踩到會呼叫 `sub_1004E`（重設 EGA 模式）| `TestUnreachableJumpIndex` 守著 |
| 設施跳表第 3 格 `0x1A2C0` | `docs/re/72` §3 早就定名（Ranger Center 的角色管理），`29` 與 `70` 沒跟上 | 文件修好了 |

**這一輪最重要的不是那六個答案，是找到答案的方法出過錯。**
`CURS` 那一格卡了三份筆記，理由是「全檔找不到讀它的程式碼」——
而掃描器本身有洞：IDA 把位址寫成負位移（`[bx-81F5h]`）時，
拿字串去 grep 一筆都對不上。**正對照 `ds:A4E0h` 當時同樣回零，沒有人發現。**
新的 `tools/scan_addr_refs.py` 把每個字面值帶正負號正規化成 16 位元再比，
而且**強制跑正對照**，回零就直接失敗。

⚠ 順帶確認一件互相印證的事：`ds:C05Dh` 的四個 byte 是 `I K J L`，
與 `docs/re/72` §4 從指令列字串讀出的方向鍵**完全一致**。
清單框架用 `I`／`K` 翻頁不是隨手挑的鍵。

### T35 — 攻略的 31 條機制斷言對過一輪（已完成，2026-08-18）

方向照 `CLAUDE.md` §5 的 oracle 優先序：**拿逆向去驗攻略**。
結果 [`docs/walkthrough/swm-005/mechanics-claims.md`](docs/walkthrough/swm-005/mechanics-claims.md)
——**相符 21、有出入 4、未定 6**，每一條都寫證據在哪。

四條有出入裡最值得記的是 **M-18「蠍式免疫子彈」**：
沒有免疫這種機制。護甲一律是 N 顆 d6 的減傷，`Scorpitron` 的骰數是 5、
平均吸收 17.5 點，手槍 4 骰（平均 14）打不動、LAW 火箭 10 骰（平均 35）打得動。
**玩家看到的「免疫」是高護甲的結果。**

順帶解掉一個跨筆記的矛盾：敵人資料 `+0x04` 低 4 位**一個欄位兩個用途**——
`loc_12A92` 的四個呼叫端裡兩個當護甲骰數（`docs/re/20`）、兩個當經驗值倍數
（`docs/re/32`）。兩份筆記各解一半，都沒錯（`docs/re/37` §3.3）。

⚠ 表裡多了第四種狀態 **未定**。「未定」與「有出入」要分得開：
寫成「有出入」等於宣告攻略記錯，而**沒有證據支持的否定與沒有證據支持的肯定
一樣糟**。六條未定各自寫著缺什麼，那就是下一輪的入口。

### T36 — `Hire` 真的雇得到人了（已完成，2026-08-18）

`docs/re/110` §5.2 掛著一個開口：雇用旗標在出貨資料裡一筆都找不到，
所以「按 `H` 到底能不能雇到人」是未知的。**找得到，而且有 14 筆**——
在 **section 3**，不是 section 15（[`docs/re/114`](docs/re/114-friendly-encounters.md)）。

| | section 3 | section 15 |
|---|---|---|
| 誰寫的 | **出貨資料**（42 張地圖共 365 格 nibble 3 指過來）| **執行期**（`sub_16890` 生成隨機遭遇時挑空槽並把那一格寫成 nibble 15）|
| 記錄長度 | 多數 12 bytes | 多數 12 bytes |
| 讀取路徑 | 同一支（`sub_13787` 拿 (x,y) 的 nibble 當 section 型別）| 同上 |

⚠ **上一輪只掃了 section 15，掃出零命中。** 兩個 section 的記錄格式、
長度、讀取路徑全部一樣，唯一的差別是 section 型別——所以連長度都不會露出破綻。
**「掃過了，沒有」要連掃描範圍一起寫進結論。**

`+0x09` 的完整語意也解出來了：bit0 ＝ 名字從地圖的明文名字表取、
**bit1 ＝ 這一組不敵對**、bit2 ＝ 不參與移動、高 4 位 ＝ section 17 的 NPC 編號。
bit1 的六個消費端全部同一個方向：友善的那一組不排移動計畫、不攻擊隊伍、
不算進「附近有敵人」、而且可以雇用。**隊伍一開槍就清掉那個位元**
（`0x1AF7B`，而且在命中判定之前——打不中也算數），這就是攻略 M-03
「誤傷友善 NPC 會翻臉」的機制。

接上去的四件事：

| | 做了什麼 |
|---|---|
| `game.Friendly`／`game.TurnHostile` | 友善的那一組**一輪都不會出手**；隊伍一攻擊就翻臉，而且寫回地圖記錄 |
| `Scene.updateCombat` 的路由 | **「哪一組？」開著時數字鍵要交給 `PickHire`**——這一條沒接，`H` 開得起來、按下去卻沒有任何反應，而畫面上只是選單又出現一次 |
| `showHirePick` | 清單畫出來（中英各一份）|
| `FinishEncounter` | **這一場才加入的人不算經驗值**。快照按名字記，新來的查不到就是 0，於是他自己帶進來的經驗值會被報成「這一場賺的」——雇到一個 5 級 NPC，畫面上就寫「全隊獲得 200 點經驗值」 |

驗收走正常玩家路徑：

```
tools/go.sh run ./cmd/wl-play -script "map=10:28:2,fight,H,1,E,E,E" -trace
→ 活 5/5｜JACKIE joins the party.〔JACKIE加入了隊伍。〕
```

`TestHireableEncountersInShippedData` 守著資料側那 14 個名字，
`TestHireFromKeyboardJoinsParty` 從按鍵走一遍。

⚠ **還沒做**：清單上顯示的是種類名（「人形生物」）而不是原版的專屬名字
（「Juveniles」）。規則解完了（`docs/re/114` §6：名字編號 ＝ 那一組的敵人型別、
名字表在記錄區標頭 `+0x02`），但那 124 條是明文 ASCII、不在打包字串表裡，
中文化要另走一批翻譯——列在下一步。

### T37 — 明文敵人名字中文化，專有名詞與地名改成「中文（英文）」（已完成，2026-08-18）

原版絕大多數的遭遇印的不是種類名，是**這張地圖自己的明文名字表**
（記錄區標頭 `+0x02`，`docs/re/114` §6）。重製版一律印「人形生物」，
所以整片畫面上都是同一個詞。

**328 條**，不是原先估的 124——那個數字是只掃前 2 KB 的結果。

| 層 | 做了什麼 |
|---|---|
| 抽取 | `tools/extract_monster_names.py` → `translations/source/monsters.tsv`。⚠ 表以 `0xFF` 結束，**不能拿記錄區標頭 `+0x31`（種類數）當筆數**：那是「隨機遭遇擲哪一種」的上限，不擲遭遇的地圖它是 0，用它會在半數地圖上抽到零條 |
| 解碼 | `Block.MonsterNames()`（`internal/assets`）。索引 **1 起算**，第 0 條是空的 |
| 顯示 | `CombatScene.properName`：遭遇記錄 `+0x09` 的 bit0 設著就用它。⚠ 索引是**遭遇記錄裡的型別**（`Enemy.Type`），不是 `Data.Kind`——後者是 1–5 的大分類，一張地圖上會重複 |
| 翻譯 | `translations/zh-Hant/monsters.tsv` 328 條，走與店家招牌同一套「顯示時查表」 |

**體例：專有名詞與地名寫成「中文（英文）」**（使用者定案 2026-08-18）。
理由是對得上——玩家拿著原版畫面、官方手冊或當年《軟體世界》的攻略時，
中文譯名對不回英文原名就查不到東西。普通名詞不加括號。

```
遊俠中心（Ranger Ctr.）    紅鷹（Redhawk）    少年
```

⚠ 這個體例**一定比原文長**，所以 `build_lang.py` 對 `place:`／`monster:`
兩類把格數上限從「不超過原文」放寬成「一行 38 格」——仍然擋得住離譜的長度，
不是完全不擋（`docs/spec/11` §5）。23 塊既有的招牌也一起改了。

⚠ **key 裡的控制碼要跳脫成 `\xNN`**：敵人名字用 `\x0A` 分單複數，
而翻譯檔是逐行 TSV，key 裡塞真的換行就沒辦法逐行讀。
`build_lang.py` 的 `read_tsv` 只 unescape 譯文、key 原樣保留，
所以 `lang.MonsterKey` 那一側也要跳脫。**兩邊不一致的症狀是
「每一條有單複數的名字都查不到」**——211／328 條靜靜顯示英文，沒有任何錯誤。

四道門檻：`TestMonsterNamesAreTranslated`（資料 → 目錄）、
`TestNoOrphanMonsterKeys`（目錄 → 資料）、`TestProperNounFormat`、
`TestPlaceSignFormat`。譯文條數 **4,915 → 5,243**。

### T38 — 踩到段落引用直接開手札那一頁（已完成，2026-08-18）

「下一步」表的前兩列是同一支函式：長段落只看得到尾巴、標題那行
`Paragraph 23` 還是英文。兩件事一起解掉。

**原因**：`maybeParagraph` 把正文倒進訊息視窗，而訊息視窗滿了會**自動捲到
最後一列**（`docs/re/106` —— 那是它該做的，訊息要的是「最後一行一定看得到」）。
162 段裡有 27 段放不下六列，最長的 38 列，所以那些段落一開始就從半句話讀起，
而**畫面上看不出來**：看起來像翻譯本來就是那樣。

**做法**：踩到引用就 `openJournal(n)`。手札那條路本來就有手動捲動
（T30 做的，捲動鍵見 T40）。

⚠ 順手補上一件本來就該有的事：**打開手札會先把訊息視窗上的東西收起來，
關掉時放回去**。以前是關掉就清空，讀完一段回到地圖是一片空白，
剛剛踩到那一格說了什麼再也看不到。現在原版那句 `Read paragraph 23.`
只是被蓋著，ESC 之後回來。

那行英文的 `Paragraph 23` 一起消失了——它在 `showParagraph` 裡，
而**那支函式沒有任何呼叫端**。刪掉，不是翻譯它。

`TestParagraphOpensJournalAtTop` 守著三件事：開的是對的那一段、
`journalScroll` 是 0（畫面第一個字就是段落開頭）、ESC 之後地圖那句話回來。

### T39 — 卡彈的武器名與生病的隊員反白了（已完成，2026-08-18）

`docs/re/111` 解出 `sub_19E2A` ＝ 反白開（`ds:4678h`），兩個條件：
角色記錄 `+0x28` 的狀態位元非 0 → **`MAX`（上限）那一欄**；裝備武器附屬
byte 的 bit7（卡彈）→ 武器名。旗標本身早就接了（`AMM` 印 0、`Load` 清彈匣），
**只有視覺標記沒接**。

`RosterRow` 多兩個旗標，`InverseAt(lay, maxCON, weapon)` 算欄位範圍，
兩條繪製路各拿去用：

| 路 | 怎麼畫 |
|---|---|
| 低解（英文）| `Frame.DrawLineInverse` → 既有的 `DrawGlyph(..., inverse)` |
| 高解（中文）| `HiFrame.FillCell` 塗滿整格，再用**背景色**畫字 |

⚠ **兩條都要補**：高解那一層原本只有「畫上前景色」，沒有背景像素可以取反
（原版是 `lodsb ／ not al ／ stosb`）。只補一條會變成
「英文畫面看得到卡彈、中文畫面看不到」。

⚠ **反白範圍要用「這一次真的要畫的那段字」算**：`30` 兩格、`100` 三格，
拿英文的長度去畫中文那一行會反白到隔壁欄。中文那一版是排版函式組出來的
一整行字串，所以照欄座標切回來（`rosterFieldsCJK`）——與排版共用同一組常數。

⚠ 只反白欄位本身的字，不含後面的補白：欄與欄之間是把游標欄寫進 `ds:4672h`
（`0x17105`、`0x17122`、`0x1715D`），中間那幾格根本沒有被印過。

三道門檻：兩個旗標的條件、欄位範圍（中英各一組）、中文那一行切得回兩欄。

⚠ 欄座標與反白欄位在 T40 修正過，見那一節。

### T40 — 手札改用 Page Up／Page Down，名單反白反錯欄（已完成，2026-08-18）

使用者玩過之後回報三件事，兩件是同一份程式碼的：手札的捲動鍵、
名單的反白範圍、生病的人反白錯欄、中文武器名被截斷。

**捲動鍵**：`↑`／`↓` 換成 `Page Up`／`Page Down`。輸入層多一個
`input.Scroll`（`ScrollNone`／`ScrollUp`／`ScrollDown`），**不共用 `Dir`**——
方向鍵在地圖上是走路、在清單上是選項，共用一個欄位的話漏判的症狀是遠處的
怪事（按 Page Down 隊伍往南走一格）。一次捲**一頁少一列**，跨頁的那句話
才接得起來。

**反白錯欄**：把 `sub_1708B`（`0x1708B`–`0x17190`）整支倒出來讀，順序是
決定性的：

```
0x170F6  bl←0x28；狀態 != 0 → sub_19E2A   ; 反白開
0x17105  ds:4672h ← 0x18                  ; 游標欄 → MAX
0x1711C  sub_176C5                        ; 印 MAXCON（16-bit）
0x1711F  sub_19E30                        ; 反白關
0x17122  ds:4672h ← 0x1C                  ; 游標欄 → CON（已經關掉了）
```

→ 狀態位元反白的是 **`MAX` 欄**，不是 `CON` 欄。同一段也回答了範圍：
欄與欄之間是把游標欄寫進 `ds:4672h`，中間那幾格**沒有被印過**，
所以反白只涵蓋欄位本身的字（原本寫「取保守的一種」，現在是讀出來的）。

⚠ 反錯一欄在畫面上照樣有一塊反白，只是位置差一格——**沒有任何症狀**。
`TestInverseSpans` 現在正反都驗：`MAX` 那幾格要反白、`CON` 那幾格一格都不准。

**中文武器名被截斷**：中文名單另立一套欄座標（`cjkRoster`），
英文那一套一格都沒動。

| | 名字 | AC | AMM | MAX | CON | 武器 | 總寬 |
|---|---|---|---|---|---|---|---|
| 英文 | 15 | 4 | 3 | 4 | 4 | 7 | 39 |
| 中文 | 12 | 3 | 3 | 3 | 3 | **14** | 40 |

三個數字都是量出來的，不是估的：

- **武器 14 格** ＝ `translations/zh-Hant/exe-skills-items.tsv` 裡最長的單數
  名字「M1989A1 北約突擊步槍」。給 13 格的話它變成「M1989A1 北約突擊步」，
  而畫面上看起來只是「這把武器叫這個名字」。
- **數值欄各 3 格**：`AMM` 是 `slot.Value & 0x3F`（0–63，兩位數封頂），
  中文表頭全部兩個字。
- **名字 12 格**：原版的名字欄位只有 13 bytes，出貨隊伍最長的是
  `Snake Vargas`（12）。
- **總寬 40** ＝ 整個畫面的字元欄數。英文那一版停在 39，最後一格原版沒有用到；
  中文用掉它，名單那幾列上沒有別的東西會被蓋到。

`TestCJKWeaponColumnFitsEveryItemName` 逐條驗全部物品名放得下——
譯文變長時由它擋下來，不然又會靜靜地被切掉。
`TestCJKRosterRowColumnsHoldAtFullWidth` 驗滿載時各欄還在自己的欄座標上
（`rosterRowCJK` 是照順序往後接的，前一欄溢出會把後面每一欄往右推，
而表頭不會跟著推）。

### T41 — 肖像框畫的是誰解完了，順手把說明那一行補上（已完成，2026-08-18）

「下一步」第 1 順位。`docs/re/105` §2 掛著「指令階段的肖像畫的是誰」——
截圖裡沒有敵人、出現的是遊俠，所以只能說「至少沒有敵人時畫的是隊伍這一邊」。

`sub_12636`（每回合呼叫一次，`0x11FEF`）把它答完了：

| 情況 | 圖 | 說明 |
|---|---|---|
| 有敵人 | 最近那一組的敵人資料 `+0x07` | 那一組的名字（`sub_129E9`，與戰鬥訊息同一支）|
| 一組都挑不到 | **8** | **字串 96 `Ranger`**（`0x12660`）|

「最近」是 `sub_126D7` 掃 `[ds:46C8h+3]`（與隊伍的距離）取最小值。
⚠ `cmp ／ cmc ／ jb` 是取最小值的慣用法——照字面讀成「大於就跳」
會把「最近的一組」讀成「最遠的一組」，而只有一組敵人的畫面上兩種讀法長得一樣。

**這一輪真正的收穫是把它從「戰鬥的元件」改成「跨模式的全域」**：
`ds:46FAh`（目前這張圖）與 `ds:7201h`（13 bytes 說明，＝ 存檔 `+0xD0` 的地點名）
換地圖時搬進 `ds:CE49h`／`ds:CE4Ah` 再搬回來，設施畫面用的是同一組。
戰鬥只是最後一個寫它們的人。

remake 這一側（`internal/play/portrait.go`）：

- 圖與說明**走同一個判斷**（`firstEnemy`）。兩邊各記一份的話，敵人死光之後
  圖還是敵人、字已經是遊俠，而畫面上看起來完全正常。
  原本的 `Scene.portrait` 欄位因此刪掉——一個真相就好。
- 說明置中在 12 格裡（`0x126A5` 的 `bl ← 6 − len/2`）。實機截圖的 `Ranger`
  六格、起點 3，對得上。
- 中文譯名是「中文（英文）」體例，比 12 格長時**先退成中文那一段**，
  還是放不下才截斷——直接截的話「掠奪者（Marau」看起來只是「名字比較長」。

⚠ **挑「最近的那一組」不適用**：敵人在地圖上移動還沒實作（下一項），
每一組與隊伍的距離不存在，所以取第一組還活著的。只有一組時兩者等價。

順帶把攻略 M-14（敵人像沒出現就無法攻擊）的前半解掉：挑組的同一個迴圈
（`sub_12284`）也負責印螢幕上那份敵人組清單，所以「像沒出現」與「清單是空的」
是同一件事。**後半仍未定**——`ds:A420h` 只有肖像在讀，「不能打」是另一個判準。

### T42 — 敵人真的會在地圖上追過來了（已完成，2026-08-18）

「下一步」第 2 順位，也是最後一項使用者指定的修正。`docs/re/101` §7 寫著
「三條分支目前接不上去，實作地圖移動的那一輪照著接即可」——這一輪就是那一輪。

RE 補齊的是「照著接」需要的四件事（`docs/re/116`）：

| 缺的 | 解法 |
|---|---|
| 步向的**實際位移** | `ds:AAB1h` 九個近指標 → 四支重疊的短常式、**八個進入點**。0–3 與隊伍走路的方向編號是同一套 |
| 朝向／背向怎麼換成步向 | 先算兩軸正負號，再查 `ds:AA05h` 的 3 × 3；斜的走不通就退成單軸，三個都不行就不動 |
| 「換位置」挑哪一格 | 九格全掃，取**分數最小**的（相等也跳過，所以正交比斜向先贏）|
| 分數是什麼 | `sub_156CB` → 武器類別的射程帶（0／1／2）→ `sub_139CE` 的一條直線 `起始 ＋ 增量 × ⌈距離／3⌉` |

⚠ **兩個方向差點記反**，都在同一種慣用法上：

- `sub_19C69` 的 CF 是「`46BEh ≥ 46C0h`」，配 `jnb` 就是**小於**才跳。
  所以那道閘是「行動值 ＋ 種類常數 **＜** 現在這一格的分數 → 換位置」，
  忍得下去就留在原地。寫反的話遠處的遠程敵人會一直逼近——
  而畫面上看起來只是「它很積極」。
- 距離表 `ds:CD0Dh` 是 **10 × 歐氏距離**（(1,1) ＝ 14、(3,4) ＝ 50），
  不是棋盤距離。而且**它早就在 `internal/game/encounter.go` 抄過一份**了
  （`game.Distance`）——差點抄第二份，同一張表兩個副本遲早會漂。

remake 這一側：`game.PlanEnemyMove` 只算「往哪一格走」，一個 byte 都不改；
執行在 `internal/play/enemymove.go`（清舊格 → 寫新格 → 更新遭遇那一格的座標
→ 印那一句話）。順序照原版：**算計畫 → 下令與結算 → 執行移動**，
所以這一回合的命中基礎值用的是這一回合的計畫。

⚠ **移動是「把那一格搬過去」，不是「加一個新的」**：舊格清成 nibble 0。
少了清舊格那一步，地圖上會留下一個永遠打不完的遭遇——
而畫面上看起來只是「這裡又有敵人」。

接上之後 `Battle.MovePlan` 不再永遠是 `NoMovePlan`，
**命中基礎值 60 那一支自己亮起來**（`docs/re/101` §7 就是為了這一天寫的）。
出貨資料實測：地圖 4 的 (18, 2) 那一組拿遠程武器，隊伍站三格外時計畫是
「換位置往右上」，`TestEnemyClosesInOverARound` 端到端守著這件事——
單元測試餵的是自己組的 `MoveEnv`，餵錯了照樣全綠。

### T43 — 設施買賣的實機對拍（已完成，2026-08-20）

「下一步」第 1 順位，`WORKLIST` 7.2 掛了很久的最後一塊。做法是把隊伍搬到
高池鎮商店門口，原版與 remake 走同一串按鍵，逐張截圖比。

**第一步就卡住**：搬過去的存檔，原版讀進去卻出現在世界地圖上。
追下去是 `docs/re/117` §1——原版讀檔只把全域狀態那 14 bytes 抄回 `ds:464Eh`，
接著用 `ds:4655h`（相對位移 7）決定載哪一張地圖，**隊伍槽表的 `+0x0A` 它一眼都沒看**。
remake 只寫槽表，於是原版用「舊地圖 ＋ 新座標」開場。
同一塊還有兩格也補上了：`ds:4653h`（這一組幾個人）與 `ds:4656h`
（已用掉的最大角色記錄編號）——雇用來的隊員只寫槽表不寫這兩格的話，
**原版讀檔時他不在隊伍裡**。

進得去之後，對出四類差異：

| 差在哪 | 原版 | 修法 |
|---|---|---|
| **版面** | 進店就切成名單模式：左邊肖像框、右上選單區（欄 15–38）、底下隊伍名單、指令列照留 | `msgRect`／`rosterLastRow` 把設施與戰鬥視為同一種版面；招牌畫在肖像框的說明位 |
| **內容** | 高池鎮商店不賣 `Engine` | 物品表有四份，商店按記錄 `+0x06` 換一份（`docs/re/118`）|
| **流程** | 先招呼、再問 `Who wants to enter?` | `StepWho` ＋ 招呼語（記錄 `+0x05` 指到這張地圖自己的字串）|
| **細節** | `P` ＝ `POOL MONEY`、清單滿頁寫 `MORE!`、價錢右對齊沒有 `$` | `game.PoolMoney`、`addMore`、`%d) %8d  %s` |

⚠ **`docs/re/42` 有一句錯了兩個月**：商店的 `P` 記成「換下一個人」，
而實機畫面外框下緣就寫著 `POOL MONEY`——`sub_19B81` 讀出來是「把其他隊員
身上的錢全部搬給這個人」。**畫面上的按鍵標籤是最便宜的斷言檢查。**

⚠ 一開始拿 remake 的**乾淨資料**對 DOSBox 的**可寫副本**，兩邊的店家庫存
本來就不同，差點把真正的差異蓋掉。實機對拍要用同一份存檔
（`wl-play -rom workplace/dosbox/game -modified`）。

還沒對的：醫生與訓練師那兩支的 `P`（remake 目前是「換下一個人」，沒有實機佐證）。

### T44 — 醫生與訓練師的實機對拍（已完成，2026-08-20）

版面與商店完全一樣（T43 已經改好），差別全在**進場那幾行**（`docs/re/119`）：

| 差在哪 | 原版 | remake 原本 |
|---|---|---|
| 招呼語的位移 | 醫生與訓練師 `+0x03`、商店 `+0x05` | 三種都讀 `+0x05` |
| 進場選人 | 三種都問（醫生「誰要治療？」）| 只有商店問 |
| 醫生主選單 | **條件式**：滿血沒病只有 `Exam $25` | 一律印 `Exam / Healing / Curing` |
| `P` | 商店與醫生是集中金錢、訓練師**沒有** | 三種都是「換下一個人」|
| 訓練師清單 | 表頭 ＋ IQ／PTS／LVL 三欄，第一列 `Brawling` | 只有費用，還多一行 `skill 0` |

⚠ **位移拿錯不會壞掉，只會印出別的句子**：醫生印成「Entering the infirmary.」
（那是走進門那一步的地圖訊息）、訓練師的 `+0x05` 落在名字欄位裡。

⚠ 招呼語原文帶控制碼（開頭常是 `\x07`），逐字畫會在畫面上變成一個方框；
而且選單區只有 24 欄，原版會折行（「Welcome to the ／ infirmary.」兩列）。
兩件事都補了（`textlayout.Render` ＋ `wrapCells`）。

**外框上的標籤是免費的斷言**：`POOL MONEY` 有沒有出現，直接回答了「這一層
有沒有這個鍵」——比在程式碼裡找按鍵表快得多。

### T45 — 攻略五條未定收斂到一條，順帶修掉試玩找到的三個明顯錯（已完成，2026-08-20）

RE 側（四份筆記，逐項見接線表）：

| 攻略斷言 | 結果 | 筆記 |
|---|---|---|
| M-09 蓋氏計數器會提示 | **相符** | [`120`](docs/re/120-geiger-counter.md)：欄 38–39 的計量表 ＋ 主迴圈的滴答聲（`1d(距離＋1) ＋ 3` tick、音效 2），讀數 ＝ 視野內最近輻射格的距離 |
| M-21 對胖佛萊迪答 `NO` 開打 | **相符**（機制是毒氣）| [`121`](docs/re/121-fat-freddy-gas-trap.md)：問答分支 → nibble 1 → opcode 7 掃防毒面具 → 有面具當場開打／沒有就被毒昏搬進牢房 |
| M-13 血池食人魚 | **有出入** | [`122`](docs/re/122-blood-pool-and-gate-armour.md)：是條件閘的懲罰不是遭遇，而且那六格明寫跳過護甲吸收 |
| M-14 像沒出現就打不到 | **相符** | [`123`](docs/re/123-attack-target-range.md)：`sub_122E9` 的三個條件，其中一個是「座標在 19 × 9 的地圖視窗內」|
| M-10 廢坑兩支 M1989 | 當時**未定** | 缺口從「擲骰怎麼算」換成「廢坑是哪一張圖」——**T54 收掉**：廢坑 ＝ 資源 31 |

順帶抓到的三個錯（都不是這一輪寫的）：

- **opcode 7 的分支方向抄反了。** 旗標 `ds:CF2Ch` 初值 1、找到才清 0，
  所以 `0x1A6D4` 保持的 `bl ＝ 3` 是「有人帶著」那條。
  **程式碼與單元測試互相印證了一個錯的答案**，是資料把它抓出來的
  （`+0x03` 指到「戴上防毒面具的人及時戴上了」）。
- **`tools/scan_item_refs.py` 兩個假零。** 指標表第 0 項可以是 0（拿它推表長會讓
  整個 section 安靜消失）；腳本的閘門用「出貨地圖有沒有 nibble 6 的格子」，
  漏掉只在改寫之後才存在的腳本。腳本記錄 42 → 141 筆。
- **`docs/re/87` §2 與 `docs/re/81` 掛著「敵方打誰還沒解」**，而 `docs/re/89` §1
  早就解完也接上了（`pickEnemyTarget`）。兩處已改成指向 89。

新增兩支工具：`tools/summarize_chests.py`（寶箱逐格內容）、
`tools/summarize_gate_penalties.py`（條件閘的獎懲，含「吃不吃護甲」那一欄）。
兩支都帶逐筆正對照——寫的時候各自踩到一次「記錄長度沒夾住就讀進鄰居的字串」。

### T46 — 接線表清空：三份「解完了沒接」全部接上（已完成，2026-08-20）

| 接了什麼 | 症狀（接之前）| 筆記 |
|---|---|---|
| 條件閘扣 CON 走傷害結算 | 全檔 168 筆扣 CON 的閘裡 **105 筆該吃護甲**，remake 一律直接扣——穿好裝備在那些格子完全沒用 | [`122`](docs/re/122-blood-pool-and-gate-armour.md) |
| 接戰值 `ds:46CCh` | `game.Engagement` 的 `far` 一直傳 `false`，等於全隊永遠是近戰接戰值 | [`123`](docs/re/123-attack-target-range.md) |
| 外框 ＋ 直排 `WASTELAND` ＋ 輻射計量表 | 畫面右邊那一欄整個是空的，四周也沒有框 | [`124`](docs/re/124-screen-border-and-right-column.md)、[`120`](docs/re/120-geiger-counter.md) |

三個都是「編得過、測得過、玩得動」的缺口，畫面上看不出少了什麼。

順帶改正兩件事：

- **`docs/re/123` 原本的框架是錯的。** `sub_122E9` 不是「攻擊的目標篩選」，
  是**這一組列不列得出來**（兩個呼叫端：「Which group?」清單的每一列、
  雇用的候選掃描）；而 `No one is within range.` 印在**雇用**那條路上。
  **把那句話當成攻擊的訊息差一點就把閘接到錯的指令上**——查呼叫端才發現。
- **右邊那一欄的直排標籤是 `WASTELAND` 不是 `RADIATION`。**
  九列字模表裡第 2 列與第 7 列相同，拿 `RADIATION` 去對會得到「那不是文字」，
  拿 `WASTELAND` 去對第一次就中（兩個 `A`）。**畫出來一秒就知道**。

### T46b — 商店清單照實機對齊（已完成，2026-08-20）

列號是 `N>` 不是 `N)`（清單元件 `sub_16DB4` 印的字元，實機 `44-zoom.png`），
設施的四種清單全部改過來；中文表頭原本照抄英文的空白數，全形字一排就歪掉，
改成把「價格」蓋在 8 格寬的價錢欄中間、「物品」對齊品名的第 13 欄。

### T47 — 畫面終於有框了（已完成，2026-08-20）

| 接了什麼 | 筆記 |
|---|---|
| 地圖畫面的外框（列 0–17 主框 ＋ 訊息視窗那一段的欄 0／39）| [`124`](docs/re/124-screen-border-and-right-column.md) |
| 右邊那一欄：直排 `WASTELAND` ＋ 輻射計量表 | [`124`](docs/re/124-screen-border-and-right-column.md)、[`120`](docs/re/120-geiger-counter.md) |
| 名單框（列 14–23、欄 0–39）＋ 雙倍高的橫幅 | [`125`](docs/re/125-roster-box.md) |

順帶改正兩個座標，兩個都是「畫面上看起來像排版風格，不像錯」的那一種：

- **成員列 ＝ 序號 ＋ 15**，所以表頭在列 15、第一個成員在列 16。
  remake 的 `RosterHeaderRow` 是 14，整份名單往上移了一格。
- **一行的游標起點是欄 1**（`sub_1738A`），序號在欄 1–2、名字從欄 3 起；
  欄 0 是框線。`AC` 以後的欄座標是**絕對值**，不跟著行首移動——
  當成相對位移會整批右移一格。

中文那一套欄座標跟著右移一欄、最右欄讓給框線，順帶把試玩回報的
「長譯名頂到畫布最右緣」變成框內的截斷。

### T48 — 框邊上的標籤（已完成，2026-08-20）

`ESC`、`POOL MONEY`、`ROSTER ON`／`OFF` 那幾個藍底小標籤接上了（[`126`](docs/re/126-box-labels.md)）。
它們**不在執行檔的字串表裡**——`grep POOL` 一筆都不中，而那個零命中
與「這東西不存在」長得一模一樣。實際上是 `ds:CA70h` 起的彩色字型**字模串**
（`(ch & 0xDF) − 0x29`、空白 `0x33`、頭尾各一個蓋子），
位置在 `ds:CBBDh` 的版面表（17 筆 × 6 bytes）。

兩個解讀是反推出來的：

- **欄與列**：拿「右邊那兩個箭頭在欄 39」當錨，一比就定了。
- **`+0x03` 是那個按鈕代表的按鍵**：把它與標籤字面並排就看出來了
  （`ESC`→`0x1B`、`ENTER`→`0x0D`、`POOL`→`0x50`、`DIVIDE`→`0x44`）。
  順帶再一次印證 `P` ＝ 集中金錢（`docs/re/42`）。

⚠ 畫出來要補 `+0x1C` 換成冷色那一組（`ds:722Fh` 預設 0），
否則標籤是紅黃色的，而畫面上只是「顏色不太一樣」。
⚠ `ROSTER ON`（列 17）與 `ROSTER OFF`（列 23）是**兩筆記錄**，不是同一個按鈕換字。

### T49 — 名單模式的兩圈框，與「那時候沒有時鐘」（已完成，2026-08-20）

`sub_19770`（肖像框 (0,0)–(13,13)）與 `sub_19727`（選單框 (14,0)–(39,13)）
各三行，都轉給通用畫框（[`127`](docs/re/127-roster-mode-boxes.md)）。
`sub_19727` 順便設文字區：欄 15–38、**列 1–12**。

三處跟著改：

- **`PanelHeight` 13 → 12**：列 13 是框的下緣，`POOL MONEY`／`MAP` 印在那條線上。
  多算一列的話最後一行字會壓在框線與標籤上。
- **下緣標籤按模式換**：戰鬥是 `MAP`、設施是 `POOL MONEY`（訓練師沒有）。
- **名單模式不畫時鐘**：兩張實機截圖上那條上緣線都是完整的。
  `docs/spec/24` 原本寫「戰鬥中照畫」，已改寫成正確答案。

「這個東西在這個畫面上有沒有」用截圖回答最快——時鐘畫不畫，
程式碼要追好幾層，兩張實機截圖擺在一起一秒就有答案。

### T50 — 框邊標籤變成按鈕（已完成，2026-08-20）

熱區表 `ds:CAEBh` 是 **21 筆 × 10 bytes**（x1／y1／x2／y2／處理常式），
前 4 筆是四塊大區域（名單、地圖視窗、圖片、指令列），
**後 17 筆與版面表一筆對一筆、順序相同、共用同一支常式 `0x8BFF`**——
那支做的事就是送出版面表 `+0x03` 那個按鍵。`docs/re/126` §3 的「未解」清掉。

remake 這一側：`BoxLabel.Key`／`Hit`，`boxLabels()` 是**繪製與滑鼠共用**的那一支
（兩邊各列一份會漂成「看得到但點不到」，而那沒有任何症狀），
`translateMouse` 裡排在 `charAt` 前面——標籤畫在框線上，那幾格底下沒有可點的字。

### T51 — 存檔的序號與「寫回哪一份」（已完成，2026-08-20）

`sub_18801` 讀完（`docs/re/30` §5.1）：

- **序號每次存檔 ＋1**（`0x18839` 的 `inc ds:7226h` ＋ 進位再 `inc ds:7228h`）。
  讀檔時兩份比序號取新的，所以**不推進就等於存了可能讀不回來**，
  而存檔當下畫面上一切正常。remake 的 `WriteSave` 現在每次先 `BumpSerial`。
- **寫回哪一份看磁碟旗標 `ds:9168h`**，而那個旗標是**資源載入器**設的
  ——也就是「目前這張地圖住在哪一個檔案」。原版沒有「上次寫哪份、這次換另一份」
  那種邏輯：**兩份輪替是副作用不是排程**。
  順帶把 `docs/re/05` 掛著的「這個旗標由誰設定｜未追」也清掉。

**重製決策**：remake 寫回讀進來的那一份，不跟著地圖的檔案跳——
序號照原版推進，`PickNewer` 一定挑得到最新的，而玩家的兩個檔案不會互相蓋掉。

### T52 — 蓋氏計數器會滴答了（已完成，2026-08-20）

原版在地圖主迴圈每個 BIOS tick 看一次讀數（`docs/re/120` §4）：`0xFF`
（視野裡沒有輻射格 或 全隊沒帶計數器）整段跳過，否則倒數遞減，歸零播音效 2、
間隔重設成 `1d(讀數＋1) ＋ 3` 個 tick——**讀數越小上界越小，滴答越密**。

remake 的音效層原本只有「這一幀誰觸發了音效」，而滴答沒有觸發事件，
所以缺的是一個**週期性的來源**：`Scene.TickGeiger` 收一次算一個 BIOS tick，
分頻交給呈現層（`internal/ui/ui.go` 那個 60 TPS ÷ 3 ＝ 20 Hz 的計數，
與設施動畫共用）。名單模式不跑——那段程式碼在地圖主迴圈裡。

### T53 — 名單上「輪到誰」的那塊反白（已完成，2026-08-20）

從實機截圖對拍發現的缺件：`43-menu.png` 裡站在櫃檯前那個人的行首序號是反白的，
而 remake 一格反白都沒有。追下去是一個獨立的旗標 `ds:471Fh`
（新筆記 [`128`](docs/re/128-roster-index-inverse.md)）：

- **三個進入點共用一個名片行繪製**：`sub_17029` 開、`sub_1702E` 關、
  `sub_17033` 沿用。`0x17033`／`0x17037` 那一對看起來是原地寫回，
  其實是另外兩個入口跳進第二條指令。
- **反白只蓋序號與 `>`**：`0x17094` 開、印完序號 `0x170A5` 就關。
  這修正了 `docs/re/111` 原本記的「整行」。
- **六個呼叫端形狀一致**：戰鬥指令階段正在下令的人、`USE` 選中的人、
  商店／醫生／訓練師櫃檯前的人，以及按數字選人之後的金錢畫面。

順帶修掉一個對拍出來的差異：**「誰要進去？」那一步不該有 `POOL MONEY`**
（`42-shop.png` 的框下緣是空的，`43-menu.png` 才有）——那個鍵屬於櫃檯前那個人。

### T54 — 攻略斷言全部對完：「廢坑」就是資源 31（已完成，2026-08-20）

最後一條未定（M-10：廢坑有兩支 M1989）收掉了，**31 條剩「相符 26、有出入 5、未定 0」**。

答案不在寶箱表裡，在**傳送表**：針岩城（資源 26）有兩個入口通到資源 31，
訊息分別是「進入鎮上的廢料坑」與「彈藥掩體裡又涼又暗」——**廢坑與彈藥庫是同一張圖**，
所以 p.57 的手繪地圖才會把兩個標註畫在城的兩側。對上之後六筆寶箱逐項吻合：
記錄 1 是 **M1989A1 ×2** ＋ 兩種彈匣，記錄 0 是 **Rad suit ×2** ＋ 防彈衣，
記錄 5 是紅寶石戒指與血杖。全圖**只有一格輻射**，正好是「走到大房間前計數器突然叫起來」。

件數是這一輪才讀出來的：`tools/summarize_chests.py` 現在把記錄裡每個物品後面那個
byte 也印出來，`×N` 是出貨寫死的、`×1dN` 是踩到才擲（bit7，`0x1530F`）。
**沒有件數就答不了「兩支」這種問題**——那一欄一直在資料裡，只是沒被印出來。

### T55 — 畫面上少的那四個箭頭，與它們牽出來的翻頁 bug（已完成，2026-08-20）

拿實機截圖對拍時發現買清單右邊少了兩個捲動箭頭。追下去不只補了箭頭，
還把 `docs/re/126` 掛著的「哪一個畫面畫哪幾筆仍然未解」整個收掉
（新筆記 [`129`](docs/re/129-label-masks.md)）：

- **每個畫面進迴圈前寫一次 32-bit 遮罩**（`ds:7DF3h`／`ds:7DF5h`），
  bit n ＝ 熱區 n，而熱區 4 起與版面表一筆對一筆。`sub_18A4F` 拿它與
  「現在畫著的」取 XOR，**只畫差異**。全檔 54 處立即數逐處解出來，
  工具那一段接在 `tools/summarize_box_labels.py` 後面。
- **前四筆（箭頭）是直的**：都在欄 39，橫著排會排到畫面外；
  原版每畫一個字模就 `inc 列 / dec 欄`，後者抵銷繪製常式的欄前進。
  所以版面表的「長度 2」對這四筆是兩列高。
- **`ROSTER OFF` 只屬於地圖畫面**——戰鬥與設施的遮罩都沒有那一位，
  實機兩張截圖的列 23 也只有框線。remake 之前兩種畫面都畫，多了一個按鈕。

順帶修掉一個實際擋路的 bug：**訓練師的技能清單翻不了頁**。
它掛在主迴圈上（`Step` 一直是 `StepMain`），而翻頁判準原本是「不在主迴圈」——
36 個技能只摸得到前 9 個，而畫面上只是「清單就這麼長」。
是遮罩表裡「訓練師清單也有上下箭頭」那一列問出來的。

## 下一步的順序

**沒有一項擋著把遊戲玩完。** 剩下的分四類：半成品介面／中文化、實作（逆向已完成）、驗收、未解逆向。

| 順位 | 類別 | 項目 | 為什麼排這裡 |
|---|---|---|---|
| 2 | 版面決策（要使用者定案）| 中文的**行距**| 量過了：倚天 24 點漢字的墨跡填滿 24 × 24 整格，而字元格就是 24 px（960 × 600 ＝ 320 × 200 的三倍），**所以行距只能是 0**。半形 ASCII 的墨跡是第 2–21 列（上下各留 2 px），看起來比漢字高就是這個差。要有行距只有兩條路：換 15 點字（`blit` 會把 16 × 15 置中，自然留 4 px），或把畫布加高讓字元格 > 24 px（那會拆掉「三倍」這個關係）。**兩條都是版面決策，不是 bug** |
| 3 | 驗收 | 開視窗的人工試玩 | 無頭試玩第二輪的三個明顯錯已修（T45、T46）。外框、計量表、護甲吸收都是這一輪新接的，要有人實際玩一輪 |
| 7 | 交付 | **`tools/dist.sh` 沒重跑**（可散布包）| 畫面、戰鬥與設施都改過好幾輪了。要出一版給人玩再跑 |

### 無頭試玩第二輪剩下的小瑕疵（2026-08-20）

三個明顯錯（設施訊息壓到名單、買不起沒回饋、一整輪只剩最後一行）已在 T45 修掉。
下面這些看得出來但不擋玩：

| # | 症狀 | 線索 |
|---|---|---|
| 1 | ~~名單的中文武器名頂到畫布最右緣~~ | **T47 修掉**：名單整套右移一欄、最右欄讓給框線，武器欄變成框內的截斷（`docs/re/125`）|
| 2 | ~~商店清單的表頭沒對齊欄位~~ | **T46 修掉**：中文表頭照抄了英文的空白數，而中文是全形字 |
| 3 | ~~店內可按什麼畫面上沒寫~~ | **T48–T50、T53 修掉**：`ESC`、`POOL MONEY`、`ROSTER OFF` 都畫在框邊上了，而且是可以點的按鈕。底下那一列指令列**照原版留著**（`docs/re/117` §2 的實機截圖，設施畫面指令列照常在）|
| 4 | 中文行距 0，長段落與九列清單會黏成一片；ASCII 列反而有留白 | 中文那條路徑的行高。`walkBody` 走的是字元列，倚天 24 點字模剛好塞滿一列 |
| 5 | 倚天半形 ASCII 的基線比全形高約 6 px，混排時字看起來浮在上面 | 同一列的 `Hell Razor` 與 `，選擇：` 對照得出來 |
| 6 | 清單折行後第二行縮排比第一行多 | `wrapCells` 沒有保留首行的縮排 |

### 候選：把長名字搬進記錄的 `0x4D`–`0x7F`

`docs/re/109` 證明那 51 bytes 沒有主人。搬進去就**不需要 `WLNAMES` 側車檔**，
玩家的存檔只有一個檔案。

⚠ **先做保存實驗再決定**：把資料寫進去 → 用原版 DOSBox 跑一輪 → 存檔 →
讀回來比對。原版沒有寫入點，但**建立角色時那筆記錄的內容從哪來還沒讀**
（`sub_1C6C9` 只寫它認識的欄位，沒有整筆清零）。實驗不過就維持側車檔——
現在這樣已經能用，不是缺口。

### 已定案：`translations/source/` 進版控（2026-08-17）

原本整個目錄被 gitignore（理由：抽出來的原版英文文本不散布），
代價是**乾淨 clone 跑不動 `tools/build_lang.py`**——
`extract_strings.py` 只重建 `exe.tsv` 與 `blocks.tsv`，
而 `ui.tsv`／`places.tsv` 是手維護的，一旦遺失就補不回來。
`.cat` 有進版控所以遊戲照跑，**這個缺口一直沒有症狀**。

使用者定案：**四份都進版控當譯文的佐證**。

⚠ **這與「進不進可散布包」是兩件事。** `tools/dist.sh` 仍然擋著
`release/translations/source`——給玩家的那一包只放譯文與目錄，原文不放。

⚠ **改完畫面要重跑三樣**，而且三樣都不會讓任何測試變紅：

```bash
tools/readme_shots.sh      # README 的九張截圖
bash tools/promo/shots.sh && tools/promo.sh    # 推廣片
tools/dist.sh              # dist-all/（會把上面兩樣複製進去）
```

譯文條數改了也要一起改字面值：`tools/promo/make_promo.sh` 兩處、
`tools/dist.sh` 的 release README 一處。`build_lang.py` 的輸出就是那個數字。
