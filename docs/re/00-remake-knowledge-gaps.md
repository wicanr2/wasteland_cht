# 00：Remake 知識缺口盤點

日期：2026-08-15 ｜ 這份是 **RE 完成度的單一檢查表**。
`CLAUDE.md` 的閘門規定「規格 READY 才能寫引擎」，而這份表回答的是
**「還差哪些規格」**。每一列都要走到「已確認」才算 RE 完成。

進度：IDA 自動辨識 **641** 個函式，人寫的筆記涵蓋 **391** 個，**250 個尚未碰過**
（`00-function-index.md`）。資料格式、文字與資產層、規則層與世界互動層都已打通；
剩下的缺口集中在**沒有進遊戲主線的東西**（`CURS`、`TRANSTBL`、`ALLPICS` 的參數區）。

推論等級：`已確認`／`強證據`／`假說`／`未解`。

---

## A. 資料與資產

| # | 項目 | 狀態 | 依據／入口 |
|---|---|---|---|
| A1 | 執行檔解包（EXEPACK） | **已確認** | `docs/re/02` |
| A2 | 資源定址（目錄＋位移表） | **已確認** | `docs/re/06`、`07` |
| A3 | MSQ 加密（XOR ＋ checksum） | **已確認** | `docs/re/08`（42/42） |
| A4 | Huffman 解壓 | **已確認** | `docs/re/11`（173 塊） |
| A5 | 地圖資料（三層） | **已確認** | 正方形，邊長 D 在記錄區標頭 `+0x2C`（32 或 64）。第 1 層 4 bits／格 ＝ section 型別、第 2 層 1 byte／格 ＝ 記錄編號、**第 3 層（Huffman 尾段）1 byte／格 ＝ 畫面上的圖形編號**（0–9 是 `IC0_9.WLF`，≥10 是圖磚編號 −10）。前兩層填滿地圖區 42/42、尾段 ＝ D² 42/42、圖形編號在範圍內 42/42（`docs/re/24`） |
| A6 | MSQ 記錄區 | **已確認（容器）** | 記錄區起點是地圖區之後（`0x600` 或 `0x1800`，不是 `0x800`），標頭 0x5C bytes，41/42 個區塊的第一個 section 落在 `P+0x5C`。取記錄走 `sub_17CB1` 的兩層索引（section 位移表 `ds:B9E0h` → 指標陣列 → `ds:46AEh`）。型別 3／5 的指標陣列結構 40/42、39/42 成立（`docs/re/16`）。區塊完整佈局（含字串表位置）見 `docs/re/18` §3 |
| A7 | 名稱字串表 | **已確認** | MSQ 區塊標頭 `+0x02` 指到明文 NUL 分隔的敵人名表（`docs/re/16` §2）；單複數用 `\n` 分隔字根／單數／複數字尾（`docs/re/17` §4.1） |
| A8 | 圖磚像素格式 | **已確認** | **16 × 16 packed 4bpp、一張 128 bytes**，存在 `ALLHTDS` 不在 MSQ 尾段。每張圖磚自帶 8 bytes 種子列 ＋ 回看 8 的 delta；載入時由 `sub_10088` 轉成 EGA 4 平面。9/9 個圖磚組長度整除 128（`docs/re/24` §3） |
| A9 | `ALLPICS` 圖形與參數表 | **已確認（圖）／未解（參數）** | 圖片是 **96 × 84 packed 4bpp ＋ 列間 XOR delta（回看 48 ＝ 列寬）**，共 82 張（33 ＋ 49）；解碼在 overlay slot 2（`sub_10144`），`tools/decode_pic.py` 已重現。交錯其中的變動長度參數區（430–2,490 bytes，`sub_10A7A` 拆成兩張指標表）**未解**（`docs/re/23`） |
| A10 | `ALLHTDS` ＝ 圖磚組 | **已確認** | 9 組（4 ＋ 5），每組 66–163 張 16 × 16 圖磚。由地圖記錄區標頭 `+0x30` 選組，0–3 在 `ALLHTDS1`、4–8 在 `ALLHTDS2`（`sub_186B6`）。42 個地圖用到的編號正好是 0–8（`docs/re/24` §3） |
| A11 | `COLORF.FNT` 字型 | **已確認** | 172 字 × 32 bytes、8×8、EGA 4 平面、平面連續存放。字模已畫出並用選單字串端對端驗證；只用於選單詞（`docs/re/14` §3、`tools/dump_font.py`） |
| A12 | `MASKS.WLF`／`IC0_9.WLF` | **已確認** | 10 個 16 × 16 的疊圖（4 平面，`seg003:0x420`）＋ 10 張遮罩（一個位元一個像素，`seg003:0xDA60`），合成走 `螢幕 ← (背景 AND 遮罩) OR 疊圖`。**十個編號的語意全部綁定**：0 塗黑、1–4／6 五種敵人（`ds:AA17h`）、5 寶箱、7 隊伍、8 輻射區（只在夜間）、9 其他分隊，四條實機對拍（`docs/re/48`）|
| A12b | `CURS`（`seg002:0x7E0B`，2,048 bytes） | **未解** | 開機讀進來但還沒追到使用端。滑鼠游標 24 × 16 的存背景／還原在 overlay slot 20–22（`docs/re/04` §3）|
| A13 | `TRANSTBL`（800 bytes、15 種值） | **未解** | 未加密。名稱像轉換表，用途未知 |
| A14 | EGA 調色盤 | **未解** | 沒找到設定調色盤的程式碼，可能用 mode 0Dh 預設。已知圖片用滿 16 種顏色（`docs/re/23` §1），所以調色盤一定是 16 色 |
| A15 | 字元碼重映射（`+0x1C`） | **已確認** | 不是重映射，是**選色**：`colorf.fnt` 有兩組形狀相同、顏色不同的字模（`0x18` 暖色／`0x34` 冷色），`ds:722Fh` 選哪一組。主文字字型另有一套，索引 ＝ ASCII − 0x20（`docs/re/14`） |
| A17 | **文字編碼（5-bit 打包）** | **已確認** | 執行檔九張表 426 條 ＋ 42 個地圖區塊 4,401 條，**共 4,827 條非空全部解出**。`tools/decode_text.py`、`tools/decode_block_text.py`（`docs/re/17`、`18`）。**中文化抽原文要用它** |
| A16 | 主文字字型 | **已確認** | **內嵌在 `wl.exe`**（`seg003:0xCA60`），不是獨立檔案。128 字 × 8 bytes、8×8 單色、索引 ＝ ASCII − 0x20，含小寫；96–99 是四個箭頭（`docs/re/14` §2） |

## B. 呈現層

| # | 項目 | 狀態 | 依據／入口 |
|---|---|---|---|
| B1 | EGA mode 0Dh ＋ 列位址表 | **已確認** | `docs/re/04` §4 |
| B2 | 畫一個字元 | **已確認** | overlay slot 19（`docs/re/04` §5） |
| B3 | 清除矩形 | **已確認** | overlay slot 9 |
| B4 | 印字串（`ds:4680h`） | **已確認** | `sub_1786E`（`docs/re/06` §3） |
| B5 | 其餘的 overlay slot | **已確認** | **26 個 slot 全部有語意**。關鍵發現：`sub_10EBE`（幾乎每個畫圖 slot 開頭那一句）是「畫之前先把滑鼠游標收起來」；slot 20／21／22 是存背景／還原／防閃爍（`docs/re/04` §3）|
| B6 | 畫面版面（各視窗座標） | **已確認** | 320 × 200 mode 0Dh。**地圖／圖片視窗 288 × 128 @ (8,8)**（19 × 9 格、四邊半格裁切，隊伍固定在第 (9,4) 格）；外框在欄 0–37、字元列 0–17；**訊息視窗欄 1–38、字元列 18–23（6 行）**；隊伍名單以 `ds:46B9h` 切換，占用地圖視窗（表頭字元列 14）。座標單位對照與換算見 `docs/re/25` |
| B7 | 文字控制碼 | **已確認（17／18）** | 18 個碼的跳表兩張（`ds:CD74h` 組行／`ds:CD50h` 直繪），反白、清行、等待按鍵、插入名字、相對定位都已對上。`0x09` **帶一個 byte 參數**，切字串時不能當文字（`docs/re/14` §4.1）。`0x0A`／`0x0C`／`0x0E` 是同一套**文字變形**機制（單複數／性別／him-her-it 三選一），`0x0F` 印數量，`0x07` 開文字框（`docs/re/28`）。**只剩 `0x08` 未解，語料裡 7 次** |

## C. 遊戲狀態

| # | 項目 | 狀態 | 依據／入口 |
|---|---|---|---|
| C1 | 存檔位置與雙份輪替 | **已確認** | 兩份都是檔尾的 MSQ 資源：`game1` `0x000253C5`、`game2` `0x00028BC7`（seek 是 `cx:dx`）。比 32-bit 序號留新的（`docs/re/09` §4、`docs/re/30`） |
| C2 | 存檔內部欄位（`0x800` ＋ `0xA00`） | **已確認** | `0x800` 段 ＝ 8 × 256：第 0 筆是全域狀態（四組隊伍槽表、時鐘、序號、地點名稱），第 1–7 筆是角色記錄。checksum ＝ 0 − Σ 明文（改寫必須重算）。**`0xA00` 段 ＝ 十組按鍵巨集**（F1–F10，每組 256 bytes，不進 checksum，`docs/re/43` §6、`docs/re/30` §6）。**改寫已實機驗收**：原版讀得進去（`docs/re/49`）|
| C3 | 角色記錄結構 | **已確認** | 定址已確認：**記錄 ＝ `0x7131 ＋ 角色編號 × 256`**，經由隊伍槽表（`sub_17208` ＋ `sub_19614`）兩層間接。欄位解出名字（`+0x00`）、金錢（24-bit，`+0x15`）、MAXCON／CON（`+0x1B`／`+0x1D`）、**技能陣列（`+0x80`，30 槽）與物品陣列（`+0xBD`，30 槽）已分辨**、傷勢等級與門檻表。**23 個位移解出、127 處存取位移追不到，是下界**（`docs/re/15`） |
| C4 | 隊伍與 NPC 同伴 | **強證據** | 四組槽表各 14 bytes：`+0x00`–`+0x07` 成員的角色記錄編號、`+0x08/+0x09` 座標、`+0x0A` 地圖編號、`+0x0B/+0x0C` 傳送目的地。出廠第 0 組是四個 Ranger（`docs/re/30` §3.1）。NPC 加入／離開的流程未讀 |
| C5 | 敵方記錄（`ds:46C8h`） | **已確認** | 94 bytes ＝ 4 bytes 標頭（x／y／地圖編號／距離）＋ 3 組 × 30 bytes（10 個 16-bit 血量 ＋ 10 個行動旗標），16 筆在 `0x6B31`。血量 ＝ `⌊基礎/4⌋ ＋ 1d(基礎低位) ＋ 256 × 1d(基礎高位)`，基礎值來自敵人資料表 `+0x00/+0x01`（`docs/re/37`）。扣血與擊殺在 `sub_15A9D`（`docs/re/20` §5.1）|

## D. 規則（remake 的核心，目前幾乎全空）

| # | 項目 | 狀態 | 依據／入口 |
|---|---|---|---|
| D1 | 屬性與技能表 | **強證據** | **35 個技能名**已從執行檔解出（`docs/re/17` §4），階級 64 條、疾病 28 條同上。**學習規則已解**：技能資料 `+0x00` 一個 byte 兩個欄位——`>> 3` 是 IQ 需求、`& 7` 是基礎技能點費用；升到等級 L 的費用 ＝ 基礎 × 2^(L−1)，溢位回 `0xFF`（`docs/re/31` §3）。技能陣列在記錄 `+0x80` 起、每筆 2 bytes。**技能資料表在 `ds:BA20h`，36 筆 × 2 bytes**，第二個 byte 是檢定用的屬性（角色記錄位移）；35 條的 IQ 與費用與官方手冊列的 27 條逐值吻合（`docs/re/32` §2）|
| D2 | 戰鬥流程 | **強證據** | **兩條攻擊路徑都完整**（含敵方扣血與擊殺，`docs/re/20` §5.1）：敵方打隊伍（`0x1B04C`）與隊伍打敵方（`0x1AF52`）。兩條共用 `sub_1B108` 累加到 `ds:46C0h`，判定前綴機器碼相同、只差 `jb`／`jnb`——累加的是**隊伍成員的本事**，方向隨攻守翻轉（`docs/re/20` §2）。回合結構與行動順序（`docs/re/36`）、敵人生成與血量（`docs/re/37`）、指令階段與逃跑（`docs/re/38`）都已解。七支指令處理程式全部已解（`docs/re/38`、`docs/re/41`）；**「隊形」沒有證據顯示存在** |
| D3 | 武器傷害與命中 | **已確認** | 命中 ＝ `roll(1..100)` 對 `ds:46C0h` 累加值比較（隊伍攻擊要 `<`、敵方攻擊要 `≥`），累加值由基礎 ＋ 隊伍成員技能×3 ＋ 修正組成、夾在 100（`docs/re/20`）。傷害 ＝ **攻擊資料表**（記錄區標頭 `+0x04`，8 bytes 一筆）`+0x05` 高 4 位 ＋ `+0x03` 顆 d6（`docs/re/32` §8.1）。護甲吸收 ＝ AC 顆 d6 的和（`docs/re/19` §4）。**隊伍打敵方是另一條路**：五項相加（`docs/re/20` §4.1）——第一項是**武器的傷害骰** `0 ＋（物品表 +0x06）顆 d6`、反戰車武器顆數加倍（`docs/re/45` §4），另外四項是武器技能×3 ＋ Dexterity／Strength／Luck 修正，不擲骰 |
| D4 | 遭遇生成 | **強證據** | `sub_16890`（2 個呼叫端）：讀記錄區標頭 `+0x2F`（機率分母）→ `roll(N)==1` 才觸發 → 掃 section 15 的槽位（`+0x03\|+0x05\|+0x07` 為 0 ＝ 空）→ `roll(標頭+0x31)` 挑種類寫進 `+0x03`、座標寫 `+0x00/+0x01`、`roll(9)` 決定方向。槽位上限在標頭 `+0x32`（`docs/re/00-master-index.md` §4.2）|
| D5 | 經驗值與升級 | **已確認（門檻與升級效果）** | 經驗值 ＝ 記錄 `+0x21`–`+0x23`（24-bit，累計、升級**不扣**）。**升到等級 L 需要 (L² − L) × 512**（`0x1B93D` 起，用加法迴圈 ＋ 九次移位算出來）。升級：等級寫 `+0x24`、技能點 `+0x20` ＋1、音效 4、階級查 `ds:D522h`（等級 → 階級編號）→ 印升階或同階訊息，可連升（`docs/re/31`）。**經驗值三條來源全解**：擊殺 ＝ 攻擊資料 `+0x00/+0x01` × ((`+0x04` 低 4 位) ＋ 1)、檢定成功 ＝ 骰值、地圖事件效果指定欄位 `0x21`；加法飽和在 `0xFFFFFF`（`docs/re/32` §7、§8）|
| D6 | 商店與經濟 | **強證據** | 商店由地圖記錄設定（`+0x03` 折價指數、`+0x05` 開場訊息）。**價格 ＝ 基礎價 − (基礎價 >> n)**；物品資料表在 `ds:7A31h`，**95 筆 × 8 bytes**（`+0x00` 基礎價、`+0x02` 供貨旗標、`+0x03` 物品類別）。買賣流程與扣款已解（`docs/re/22`）|
| D7 | **亂數產生器** | **已確認** | `sub_18E6B`（`0x18E6B`）：`ds:465Ch`–`4660h` 五個位元組的進位鏈，映像初值全零、全檔無種子設定，熵來自鍵盤輪詢次數。擲骰層四支：`sub_18E5F` d6、`sub_18E41` dN、`sub_19D86` 累加 Nd6、`sub_19C84` 2d6 同點續擲。模型與驗證見 `docs/re/13`、`tools/rng.py` |

## E. 世界互動

| # | 項目 | 狀態 | 依據／入口 |
|---|---|---|---|
| E1 | 移動與時間推進 | **已確認** | 一步 ＝ `sub_1651A(方向)` → 可否進入（`sub_1649E` 四道閘）→ 捲動（`ds:AAA7h` 四支，overlay slot 11–14）→ 腳步音效 → 遭遇擲骰 → 重畫與觸發（`docs/re/26`）。**時鐘是 24 小時制**：`ds:4658h`（分的小數）／`4659h`（分）／`465Ah`（時），每步加標頭 `+0x34/+0x35`（＝ 每步分鐘 × 256，荒野 4 分、室內 15 秒）。晝夜門檻 6 時與 18 時，會換圖形與記錄欄位；`ds:4722h` 每 16 刻觸發 `sub_12440` 的角色狀態處理（`docs/re/27`） |
| E2 | 地圖事件觸發 | **已確認** | 踩上去由 `ds:AA87h` 這張 **16 筆跳表**依第 1 層 nibble 分派（`sub_16410`）。7 種指到空函式、8 種有專屬處理：**1 ＝ 遠看才顯示的描述、2 ＝ 條件串列（門／鎖／檢定，`docs/re/32` §6）、4 ＝ 印一句、5 ＝ 寶箱（第一次踩到才擲出物品並寫回記錄）、6 ＝ 設施與腳本（第二層分派：5 筆設施畫面已讀出共同形狀與商店身分；另 44 筆是**腳本指令，全部讀完**——條件與晝夜分支、調整遭遇率／種類數、生成地圖物件、批次改記錄、時間戳與倒數（`docs/re/34`）、8 ＝ 多選一選單、9／12 ＝ 印訊息、10 ＝ 傳送**（`docs/re/29`）。實測資料用到的 nibble 與有實作的 nibble 吻合。**欄位語意隨 section 型別而不同**：section 15 的 `+0x03` 是遭遇種類，section 3 的 `+0x03` 像訊息編號（`docs/re/26`）|
| E3 | 段落編號顯示 | **已確認** | **沒有段落機制**：`Read paragraph N.` 就是普通敘述字串，區塊文字裡 83 處、涵蓋 82 個編號，執行檔字串表零筆。一處編號由選單選項在執行期拼上去。陷阱段落 1／22／145 **零引用**，用遊戲資料驗證了段落書的分層（`docs/re/33`）|
| E4 | 遊戲內短訊息來源 | **已確認** | 九張 **5-bit 打包字串表**，合計 444 個槽、426 條非空（`docs/re/17`）。表基址存 `ds:4692h`；`ds:29FAEh` 不是頻率表，是其中一張表的字元對照表 |
| E5 | 門／鎖／陷阱／技能檢定 | **強證據（機制）** | 地圖事件對角色的效果全部走同一支資料驅動的出口：記錄 `+0x08`／`+0x09` 描述「哪個欄位、加或減、固定值或 Nd6」（`sub_14193` → `sub_141FA`，`docs/re/19`）。**觸發條件也已解**：地圖記錄 `+0x0A` 起每 2 bytes 一個條件，第一個 byte 高 3 位是型別、低 5 位是難度——技能檢定／屬性與等級檢定／隊伍人數／金錢／持有物品五種。檢定 ＝ 2d6 續擲（< 5 直接失敗）＋ 屬性 ＋ 技能等級×3 ≥ 難度×5＋15，成功還會順便練等（`docs/re/32` §3–§6）|

## F. 系統

| # | 項目 | 狀態 | 依據／入口 |
|---|---|---|---|
| F1 | 鍵盤輸入 | **已確認** | `sub_18E90`（28 callers）是「等待按鍵」：呼叫 `sub_18EFE` 直到回非 0，並把 `0x1B`（ESC）以 CF 回報。`sub_18EFE` 同時做 **F1–F10 巨集錄放**，出口把 `a`–`z` 轉大寫——**整個遊戲拿到的字母永遠是大寫**（`docs/re/43` §6、`docs/re/46` §2.1）。文字輸入本體是 `loc_17750`（`docs/re/46` §2）|
| F2 | 音效 | **已確認** | **PC 喇叭**，`int 08h` ISR 以 126.36 Hz 驅動一台位元組碼直譯器：4 個聲部 × `0x2E` bytes、4 個 opcode、優先權仲裁（不混音）。九首資料在 `seg005`（864 bytes），不在任何外部檔案。第 6 首**設計上就是無限循環**；E 音比十二平均律低 2.91 音分，照抄不要修（`docs/re/44`）|
| F3 | 計時器中斷（`int 08h` 掛勾） | **強證據** | `sub_1CB75` 取原向量（`AX=3508h`）後把 int 08 指向 `seg001:0x00C9`（線性 `0x1CC30`，IDA 未建成函式），並用 `out 43h`／`out 40h` ×2 重設 channel 0 的除頻值（提高中斷頻率）。`sub_1CBAA` 是還原：寫回預設除頻值並關掉喇叭閘 |
| F4 | 滑鼠（`int 33h` ×5） | **已確認** | 原版有用：**21 筆熱區表**在 `ds:CAEBh`（每筆 5 word ＝ x1／y1／x2／y2／handler），游標 24 × 16，存背景／還原走 overlay slot 20–22（`docs/re/43`、`docs/spec/20`）|
| F5 | 印表機（`int 17h` ×3） | 不移植 | 原版可列印角色卡；remake 不需要 |
| F6 | 複製保護 | 不移植 | 使用者已定案（`CLAUDE.md` §3.2） |

---

## 下一步的優先序

依「對 remake 的阻擋程度」排：

1. **A9 的參數區** —— 圖片本身已解，剩下交錯的變動長度參數表（`sub_10A7A`）。
   不擋 remake：那是動畫／局部重畫用的，靜態圖已經畫得出來。
2. **A13 `TRANSTBL`、A12b `CURS`** —— 開機讀進來，但還沒追到使用端。
3. **A14 EGA 調色盤** —— 沒找到設定調色盤的程式碼；實機對拍已間接證實是 mode 0Dh 預設
   十六色（`docs/re/47` §4），但「原版真的沒有設過」還缺一次全檔掃描。

上面三項都**不擋實作**：對應的機制已經有 READY 規格，缺的是「為什麼」而不是「怎麼做」。

已完成：**D7 亂數產生器**（`docs/re/13`）、**A11／A15／A16 字型與文字編碼**（`docs/re/14`）、
**C3 角色記錄定址與欄位**（`docs/re/15`）、**A6 MSQ 區塊佈局**（`docs/re/16`）、**A17／E4 文字編碼與字串表**（`docs/re/17`、`18`）、**效果系統與傷害**（`docs/re/19`）、**命中判定與武器傷害**（`docs/re/20`）、**七個屬性、修正值階梯與角色建立**（`docs/re/21`）、**商店與物品資料表**（`docs/re/22`、`45`）、**隊伍打敵方的傷害來源**（`docs/re/45`）、
**圖片格式**（`docs/re/23`）、**A5／A8／A10 地圖三層與圖磚、第 3 層的圖形編號**（`docs/re/24`）、**B6 畫面版面**（`docs/re/25`）、**E1／E2 移動、事件分派與遊戲時鐘**（`docs/re/26`、`27`）、
**B7 文字變形**（`docs/re/28`）、**E2 的五支事件處理函式**（`docs/re/29`）、**C1／C2／C3／C4 存檔與隊伍**（`docs/re/30`）、
**F2 音效**（`docs/re/44`）、**F1／F4 輸入與滑鼠**（`docs/re/43`、`46`）、**A12 十張疊圖**（`docs/re/48`）、**存檔改寫的實機驗收**（`docs/re/49`）、
**D5 升級門檻與 D1 技能學習公式**（`docs/re/31`）、**E5 檢定條件、技能表與經驗值來源**（`docs/re/32`）、**E3 段落引用與 E2 剩下的三個 nibble**（`docs/re/33`、`29`）、**地圖腳本的 44 個指令**（`docs/re/34`）、**狀態位元與體力恢復**（`docs/re/35`）。

閘門是**逐機制**的：已確認的部分已經收攏成三份 READY 規格
（`docs/spec/00-index.md`），資產層、呈現層與亂數可以動工。
**上表還沒走到「已確認」的機制，對應的規格不會標 READY，也就不得寫成引擎程式碼**
（`CLAUDE.md` §0）。
